{
  "name": "cling-talk-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "vitest run",
    "build": "npm -w packages/client run build"
  },
  "devDependencies": {
    "vitest": "^3"
  }
}
