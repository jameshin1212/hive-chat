---
phase: 19-slash-command-responsive-finish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/ui/components/CommandSuggestions.tsx
  - packages/client/src/ui/screens/ChatScreen.tsx
autonomous: true
requirements: [SLSH-01, SLSH-02]

must_haves:
  truths:
    - "/ 입력 시 명령어 목록이 오버레이로 표시되며 명령어명과 설명이 정렬되어 보인다"
    - "오버레이에서 Enter 누르면 선택된 명령어가 즉시 실행된다 (input에 채우지 않고 바로 실행)"
    - "Escape로 오버레이 닫기, 화살표로 항목 이동이 기존과 동일하게 동작한다"
  artifacts:
    - path: "packages/client/src/ui/components/CommandSuggestions.tsx"
      provides: "Claude Code 스타일 명령어 오버레이 UI"
      contains: "borderStyle"
    - path: "packages/client/src/ui/screens/ChatScreen.tsx"
      provides: "Enter 시 즉시 실행 로직"
      contains: "handleSubmit"
  key_links:
    - from: "ChatScreen.tsx handleKeyIntercept"
      to: "handleSubmit"
      via: "Enter on suggestion triggers command execution directly"
      pattern: "handleSubmit.*selected"
---

<objective>
슬래시 명령어 오버레이를 Claude Code 스타일로 시각 개선하고, Enter 입력 시 선택된 명령어를 즉시 실행하도록 변경한다.

Purpose: 현재는 Enter로 명령어를 input에 채운 후 다시 Enter를 눌러야 실행되는 2-step 방식. 1-step으로 개선하여 UX 향상.
Output: 개선된 CommandSuggestions 컴포넌트 + ChatScreen 즉시 실행 로직
</objective>

<execution_context>
@/Users/jamie/Jamie_Dev/claude_chat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamie/Jamie_Dev/claude_chat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-slash-command-responsive-finish/19-CONTEXT.md

@packages/client/src/ui/components/CommandSuggestions.tsx
@packages/client/src/ui/screens/ChatScreen.tsx
@packages/client/src/commands/CommandParser.ts
@packages/client/src/ui/theme.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs -->

From packages/client/src/commands/CommandParser.ts:
```typescript
export const COMMANDS = {
  '/exit': { description: 'Exit HiveChat' },
  '/users': { description: 'Show nearby users' },
  '/friends': { description: 'Show friend list' },
  '/settings': { description: 'Open settings' },
  '/help': { description: 'Show available commands' },
  '/leave': { description: 'Leave current chat' },
  '/addfriend': { description: 'Add friend by nick#tag' },
  '/removefriend': { description: 'Remove friend by nick#tag' },
} as const;

export function filterCommands(prefix: string): Array<{ name: string; description: string }>;
```

From packages/client/src/ui/theme.ts:
```typescript
export const theme = {
  text: { primary, secondary, error, info },
  ui: { separator, statusBar, prompt, transition },
  badge: { ... },
} as const;
```

ChatScreen handleKeyIntercept (line 163-187): 현재 Enter 시 `setText(selected.name + ' ')` 후 return true.
ChatScreen handleSubmit (line 189-292): 실제 명령어 실행 로직.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: CommandSuggestions Claude Code 스타일 시각 개선</name>
  <files>packages/client/src/ui/components/CommandSuggestions.tsx</files>
  <read_first>
    - packages/client/src/ui/components/CommandSuggestions.tsx
    - packages/client/src/ui/theme.ts
  </read_first>
  <action>
CommandSuggestions 컴포넌트를 Claude Code 스타일로 개선:

1. 전체를 Box borderStyle="single" borderColor={theme.text.info} (cyan)로 감싸기
2. 명령어명을 고정 폭으로 좌측 정렬 (가장 긴 명령어 기준 padding). 명령어명은 color={theme.text.primary} (green) 적용
3. 설명은 dimColor 유지하되 ' -- ' 대신 적절한 간격으로 구분
4. 선택된 항목: inverse + bold 유지, 선택 indicator '>' 사용
5. 비선택 항목: '  ' (2칸 space) prefix
6. 스크롤 indicator (more items) 기존 로직 유지

시각적 목표: 명령어명이 깔끔하게 세로 정렬되고 설명이 dimColor로 우측에 표시. Box border로 오버레이 영역이 명확히 구분됨.
  </action>
  <verify>
    <automated>cd /Users/jamie/Jamie_Dev/hive_chat && npx tsc --noEmit -p packages/client/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - CommandSuggestions.tsx에 borderStyle="single" 포함
    - 명령어명에 color={theme.text.primary} 적용
    - 선택 항목에 inverse + bold 유지
  </acceptance_criteria>
  <done>CommandSuggestions가 Box border 안에 명령어명 정렬 + dimColor 설명으로 렌더링된다</done>
</task>

<task type="auto">
  <name>Task 2: Enter 시 선택 명령어 즉시 실행</name>
  <files>packages/client/src/ui/screens/ChatScreen.tsx</files>
  <read_first>
    - packages/client/src/ui/screens/ChatScreen.tsx
    - packages/client/src/commands/CommandParser.ts
  </read_first>
  <action>
ChatScreen.tsx의 handleKeyIntercept에서 Enter 키 동작을 변경:

현재 (line 174-180):
```typescript
if (key.return && filtered.length > 0) {
  const selected = filtered[suggestionIndex];
  if (selected) {
    setText(selected.name + ' ');  // input에 채움
    setShowSuggestions(false);
  }
  return true;
}
```

변경:
```typescript
if (key.return && filtered.length > 0) {
  const selected = filtered[suggestionIndex];
  if (selected) {
    setText('');  // input 클리어
    setShowSuggestions(false);
    handleSubmit(selected.name);  // 즉시 실행
  }
  return true;
}
```

주의사항:
- handleKeyIntercept는 useCallback 내부이므로 handleSubmit을 dependency에 추가해야 함
- handleSubmit이 이미 같은 스코프에 있으므로 직접 호출 가능
- /addfriend, /removefriend처럼 args가 필요한 명령어는 Enter 즉시 실행 시 args 없이 호출됨 -> handleSubmit에서 이미 "Usage:" 안내 메시지 표시하므로 문제 없음
- setText('') 호출로 input 필드 초기화

overlayHeight 계산에서 CommandSuggestions의 Box border 추가분(+2 for top/bottom border) 반영:
현재 line 78-81의 showSuggestions 분기에서 return 값에 +2 추가.
  </action>
  <verify>
    <automated>cd /Users/jamie/Jamie_Dev/hive_chat && npx tsc --noEmit -p packages/client/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - handleKeyIntercept에서 key.return 시 handleSubmit 직접 호출
    - handleKeyIntercept에서 key.return 시 setText('') 호출
    - handleKeyIntercept의 useCallback deps에 handleSubmit 포함
    - overlayHeight에서 showSuggestions 분기에 border 높이(+2) 반영
  </acceptance_criteria>
  <done>오버레이에서 Enter 누르면 선택된 명령어가 즉시 실행되고 input이 초기화된다</done>
</task>

</tasks>

<verification>
1. TypeScript 컴파일 에러 없음
2. `/` 입력 시 Box border가 있는 오버레이 표시
3. 화살표로 항목 이동, Enter로 즉시 실행 확인
4. Escape로 오버레이 닫기 동작 유지
</verification>

<success_criteria>
- CommandSuggestions가 Box border + 명령어명 정렬 + dimColor 설명으로 표시
- Enter 시 명령어가 input에 채워지지 않고 바로 실행됨
- TypeScript 컴파일 통과
</success_criteria>

<output>
After completion, create `.planning/phases/19-slash-command-responsive-finish/19-01-SUMMARY.md`
</output>
