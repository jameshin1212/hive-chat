---
phase: 09-settings-command
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - packages/client/src/ui/screens/ChatScreen.tsx
autonomous: false
requirements: [SET-01, SET-02, SET-03, SET-04]

must_haves:
  truths:
    - "/settings 입력 시 SettingsOverlay가 표시된다"
    - "닉네임 변경 후 시스템 메시지가 표시되고 서버에 재접속된다"
    - "AI CLI 변경 후 시스템 메시지가 표시되고 서버에 재접속된다"
    - "설정 화면에서 Esc로 채팅 화면으로 돌아온다"
    - "설정 변경 후 StatusBar에 새 identity가 즉시 반영된다"
  artifacts:
    - path: "packages/client/src/ui/screens/ChatScreen.tsx"
      provides: "Settings overlay integration with /settings command"
      contains: "showSettings"
  key_links:
    - from: "packages/client/src/ui/screens/ChatScreen.tsx"
      to: "packages/client/src/ui/components/SettingsOverlay.tsx"
      via: "SettingsOverlay import and rendering"
      pattern: "import.*SettingsOverlay"
    - from: "packages/client/src/ui/screens/ChatScreen.tsx"
      to: "identity prop change"
      via: "onIdentityChange callback triggers useServerConnection reconnect"
      pattern: "onIdentityChange"
---

<objective>
ChatScreen에 SettingsOverlay를 통합하여 /settings 명령어가 동작하게 만들기. 닉네임/AI CLI 변경 시 시스템 메시지 표시 + 서버 재접속.

Purpose: 사용자가 /settings로 프로필을 확인하고 변경할 수 있는 완전한 흐름 완성
Output: 동작하는 /settings 명령어, 설정 변경 시 자동 재접속
</objective>

<execution_context>
@/Users/jamie/Jamie_Dev/claude_chat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamie/Jamie_Dev/claude_chat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-settings-command/09-CONTEXT.md
@.planning/phases/09-settings-command/09-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From packages/client/src/ui/components/SettingsOverlay.tsx (created in 09-01):
```typescript
interface SettingsOverlayProps {
  identity: Identity;
  visible: boolean;
  onClose: () => void;
  onIdentityChange: (identity: Identity) => void;
}
export function SettingsOverlay({ identity, visible, onClose, onIdentityChange }: SettingsOverlayProps): JSX.Element;
```

From packages/client/src/identity/IdentityManager.ts (updated in 09-01):
```typescript
export function updateIdentity(updates: { nickname?: string; aiCli?: AiCli }): Identity;
```

From packages/client/src/ui/App.tsx (updated in 09-01):
```typescript
// App now passes: <ChatScreen identity={identity} onIdentityChange={setIdentity} />
```

From packages/client/src/ui/screens/ChatScreen.tsx (current):
```typescript
interface ChatScreenProps {
  identity: Identity;
  // Will add: onIdentityChange?: (identity: Identity) => void;
}
```

From packages/client/src/hooks/useServerConnection.ts:
```typescript
// identity in useEffect deps — changing identity auto-reconnects
export function useServerConnection(identity: Identity): { status, client, transportType };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ChatScreen에 SettingsOverlay 통합</name>
  <files>packages/client/src/ui/screens/ChatScreen.tsx</files>
  <read_first>
    - packages/client/src/ui/screens/ChatScreen.tsx
    - packages/client/src/ui/components/SettingsOverlay.tsx
    - packages/client/src/hooks/useServerConnection.ts
  </read_first>
  <action>
    ChatScreen.tsx에 다음 변경 적용:

    **1. Props 확장:**
    ```typescript
    interface ChatScreenProps {
      identity: Identity;
      onIdentityChange?: (identity: Identity) => void;
    }
    ```
    destructure에 `onIdentityChange` 추가.

    **2. showSettings state 추가:**
    ```typescript
    const [showSettings, setShowSettings] = useState(false);
    ```

    **3. overlayHeight 계산에 settings 추가:**
    settings overlay 높이: 약 8줄 (프로필 정보 3줄 + 메뉴 항목 2줄 + title/separator 3줄).
    `if (showSettings) return 8;` — 다른 overlay 조건보다 앞에 배치.

    **4. handleSubmit에 /settings 핸들러 추가:**
    `/chat` 핸들러와 같은 위치에 추가 (기존 `isKnownCommand` 체크 전):
    ```typescript
    if (parsed.name === '/settings') {
      setShowSettings(true);
      return;
    }
    ```

    **5. handleIdentityChange 콜백:**
    ```typescript
    const handleIdentityChange = useCallback((newIdentity: Identity) => {
      onIdentityChange?.(newIdentity);
      // 시스템 메시지: 어떤 필드가 바뀌었는지 비교
      if (newIdentity.nickname !== identity.nickname) {
        addSystemMessage(`Nickname changed to ${newIdentity.nickname}#${newIdentity.tag}`);
      }
      if (newIdentity.aiCli !== identity.aiCli) {
        addSystemMessage(`AI CLI changed to ${newIdentity.aiCli}`);
      }
    }, [identity, onIdentityChange, addSystemMessage]);
    ```

    **6. SettingsOverlay 렌더링:**
    기존 overlay들(FriendList, UserList, ChatRequestOverlay) 다음, CommandSuggestions 앞에 추가:
    ```tsx
    {showSettings && (
      <SettingsOverlay
        identity={identity}
        visible={showSettings}
        onClose={() => setShowSettings(false)}
        onIdentityChange={(newIdentity) => {
          handleIdentityChange(newIdentity);
          setShowSettings(false);
        }}
      />
    )}
    ```

    **7. IMETextInput isActive에 !showSettings 추가:**
    `isActive={!isInputDisabled && !showUserList && !showFriendList && !incomingRequest && !showSettings}`

    **8. MessageArea isActive에 !showSettings 추가:**
    `isActive={!showUserList && !showFriendList && !incomingRequest && !showSettings}`

    **9. import 추가:**
    `import { SettingsOverlay } from '../components/SettingsOverlay.js';`

    **중요:** identity가 변경되면 `useServerConnection`의 useEffect deps에 identity가 있으므로 자동으로 기존 연결 destroy + 새 identity로 재접속. 별도 재접속 로직 불필요.
  </action>
  <verify>
    <automated>cd /Users/jamie/Jamie_Dev/cling_talk && npx tsc --noEmit --project packages/client/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "showSettings" packages/client/src/ui/screens/ChatScreen.tsx
    - grep -q "SettingsOverlay" packages/client/src/ui/screens/ChatScreen.tsx
    - grep -q "onIdentityChange" packages/client/src/ui/screens/ChatScreen.tsx
    - grep -q "/settings" packages/client/src/ui/screens/ChatScreen.tsx
    - grep -q "Nickname changed" packages/client/src/ui/screens/ChatScreen.tsx
    - grep -q "AI CLI changed" packages/client/src/ui/screens/ChatScreen.tsx
    - grep -q "!showSettings" packages/client/src/ui/screens/ChatScreen.tsx
  </acceptance_criteria>
  <done>/settings 입력 시 SettingsOverlay 표시, 닉네임/AI CLI 변경 시 시스템 메시지 + 자동 재접속, 설정 중 입력 비활성화</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Settings 기능 전체 동작 검증</name>
  <what-built>
    /settings 명령어로 프로필 확인 + 닉네임 변경 + AI CLI 변경이 가능한 설정 화면.
    변경 시 서버 자동 재접속, 시스템 메시지 표시, StatusBar 즉시 반영.
  </what-built>
  <how-to-verify>
    1. `cd /Users/jamie/Jamie_Dev/cling_talk && npm run dev` 로 실행
    2. `/settings` 입력 후 Enter — 설정 화면이 표시되는지 확인
    3. 현재 닉네임, TAG, AI CLI가 프로필 정보로 표시되는지 확인 (SET-04)
    4. 화살표 키로 [닉네임 변경] 선택 후 Enter — 닉네임 입력 화면 진입 확인
    5. 새 닉네임 입력 후 Enter — "Nickname changed to xxx#TAG" 시스템 메시지 확인 (SET-02)
    6. StatusBar에 새 닉네임 반영 확인
    7. 다시 `/settings` → [AI CLI 변경] → 다른 CLI 선택 → "AI CLI changed to X" 확인 (SET-03)
    8. Esc로 설정 화면 닫기 확인
    9. 한글 닉네임 입력 시 거부되는지 확인 (NICKNAME_REGEX: a-z, 0-9, -, _)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit --project packages/client/tsconfig.json` — 타입 에러 없음
- `/settings` 명령어 동작: 설정 화면 표시
- 닉네임 변경: TAG 유지, 시스템 메시지, 서버 재접속
- AI CLI 변경: 시스템 메시지, 서버 재접속
- 프로필 정보 표시: 닉네임, TAG, AI CLI
- Esc로 설정 화면 닫기
- 설정 화면 중 IMETextInput 비활성화
</verification>

<success_criteria>
- /settings → 설정 화면 표시 (SET-01)
- 닉네임 변경 가능, TAG 유지 (SET-02)
- AI CLI 변경 가능 (SET-03)
- 프로필 정보 확인 가능 (SET-04)
- 변경 후 시스템 메시지 + 서버 자동 재접속
- 설정 화면 중 다른 overlay/입력과 충돌 없음
</success_criteria>

<output>
After completion, create `.planning/phases/09-settings-command/09-02-SUMMARY.md`
</output>
