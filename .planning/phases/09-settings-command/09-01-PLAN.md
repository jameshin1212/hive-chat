---
phase: 09-settings-command
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/identity/IdentityManager.ts
  - packages/client/src/identity/IdentityManager.test.ts
  - packages/client/src/ui/components/SettingsOverlay.tsx
  - packages/client/src/ui/App.tsx
autonomous: true
requirements: [SET-01, SET-04]

must_haves:
  truths:
    - "updateIdentity()로 닉네임만 변경 시 TAG가 유지된다"
    - "updateIdentity()로 aiCli만 변경 시 다른 필드가 유지된다"
    - "SettingsOverlay가 현재 프로필 정보(닉네임, TAG, AI CLI)를 표시한다"
    - "SettingsOverlay에서 화살표 키로 메뉴 항목을 선택할 수 있다"
    - "App 컴포넌트가 identity 변경 콜백을 ChatScreen에 전달한다"
  artifacts:
    - path: "packages/client/src/identity/IdentityManager.ts"
      provides: "updateIdentity function"
      contains: "export function updateIdentity"
    - path: "packages/client/src/ui/components/SettingsOverlay.tsx"
      provides: "Settings overlay component"
      contains: "export function SettingsOverlay"
    - path: "packages/client/src/ui/App.tsx"
      provides: "onIdentityChange callback to ChatScreen"
      contains: "onIdentityChange"
  key_links:
    - from: "packages/client/src/ui/components/SettingsOverlay.tsx"
      to: "packages/client/src/identity/IdentityManager.ts"
      via: "updateIdentity import"
      pattern: "import.*updateIdentity.*IdentityManager"
    - from: "packages/client/src/ui/App.tsx"
      to: "packages/client/src/ui/screens/ChatScreen.tsx"
      via: "onIdentityChange prop"
      pattern: "onIdentityChange"
---

<objective>
Settings 기능의 핵심 인프라 구축: identity 업데이트 함수, SettingsOverlay UI 컴포넌트, App에서 ChatScreen으로의 identity 변경 콜백 전달.

Purpose: SettingsOverlay가 독립적으로 동작할 수 있도록 핵심 컴포넌트와 함수를 먼저 구현
Output: updateIdentity(), SettingsOverlay 컴포넌트, App identity change flow
</objective>

<execution_context>
@/Users/jamie/Jamie_Dev/claude_chat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamie/Jamie_Dev/claude_chat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-settings-command/09-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From packages/shared/src/types.ts:
```typescript
export const AI_CLI_OPTIONS = ['Claude Code', 'Codex', 'Gemini', 'Cursor'] as const;
export type AiCli = (typeof AI_CLI_OPTIONS)[number];

export interface Identity {
  nickname: string;
  tag: string; // 4-char uppercase hex
  aiCli: AiCli;
  schemaVersion: 1;
}
```

From packages/shared/src/constants.ts:
```typescript
export const NICKNAME_REGEX = /^[a-z0-9_-]{1,16}$/;
```

From packages/client/src/identity/IdentityManager.ts:
```typescript
export function saveIdentity(nickname: string, aiCli: AiCli): Identity;
export function loadIdentity(): Identity | undefined;
export function clearIdentity(): void;
export function formatIdentityDisplay(identity: Identity): string;
```

From packages/client/src/ui/components/AiCliSelector.tsx:
```typescript
interface AiCliSelectorProps {
  onSelect: (aiCli: AiCli) => void;
}
export function AiCliSelector({ onSelect }: AiCliSelectorProps): JSX.Element;
```

From packages/client/src/ui/App.tsx:
```typescript
// Currently: <ChatScreen identity={identity} />
// Will become: <ChatScreen identity={identity} onIdentityChange={setIdentity} />
```

From packages/client/src/ui/screens/ChatScreen.tsx:
```typescript
interface ChatScreenProps {
  identity: Identity;
  // Will add: onIdentityChange?: (identity: Identity) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: updateIdentity() 함수 추가 + 테스트</name>
  <files>packages/client/src/identity/IdentityManager.ts, packages/client/src/identity/IdentityManager.test.ts</files>
  <read_first>
    - packages/client/src/identity/IdentityManager.ts
    - packages/shared/src/types.ts
    - packages/shared/src/schemas.ts
  </read_first>
  <behavior>
    - updateIdentity({nickname: 'newname'})는 기존 TAG를 유지하고 닉네임만 변경한 Identity를 반환한다
    - updateIdentity({aiCli: 'Codex'})는 기존 닉네임/TAG를 유지하고 aiCli만 변경한 Identity를 반환한다
    - updateIdentity({nickname: 'new', aiCli: 'Gemini'})는 둘 다 변경하되 TAG는 유지한다
    - 저장된 identity가 없을 때 updateIdentity() 호출 시 에러를 던진다
    - 변경된 identity가 appConfig에 저장된다
  </behavior>
  <action>
    IdentityManager.ts에 `updateIdentity` 함수 추가:

    ```typescript
    export function updateIdentity(updates: { nickname?: string; aiCli?: AiCli }): Identity {
      const current = loadIdentity();
      if (!current) throw new Error('No identity to update');
      const updated: Identity = {
        ...current,
        ...(updates.nickname !== undefined ? { nickname: updates.nickname } : {}),
        ...(updates.aiCli !== undefined ? { aiCli: updates.aiCli } : {}),
      };
      identitySchema.parse(updated);
      appConfig.set('identity', updated);
      return updated;
    }
    ```

    테스트는 vitest로 작성. appConfig를 mock하여 저장/로드 동작 검증.
    NICKNAME_REGEX 검증은 zod schema(identitySchema.parse)가 처리하므로 별도 검증 불필요 — 하지만 잘못된 닉네임에 대해 zod 에러가 발생하는지 테스트.
  </action>
  <verify>
    <automated>cd /Users/jamie/Jamie_Dev/cling_talk && npx vitest run packages/client/src/identity/IdentityManager.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export function updateIdentity" packages/client/src/identity/IdentityManager.ts
    - grep -q "updateIdentity" packages/client/src/identity/IdentityManager.test.ts
    - grep -q "tag.*유지\|tag.*preserve\|current.tag\|\.tag" packages/client/src/identity/IdentityManager.ts
  </acceptance_criteria>
  <done>updateIdentity()가 TAG를 유지하며 닉네임/aiCli를 변경하고, 모든 테스트 통과</done>
</task>

<task type="auto">
  <name>Task 2: SettingsOverlay 컴포넌트 + App identity 콜백</name>
  <files>packages/client/src/ui/components/SettingsOverlay.tsx, packages/client/src/ui/App.tsx</files>
  <read_first>
    - packages/client/src/ui/components/UserList.tsx
    - packages/client/src/ui/components/AiCliSelector.tsx
    - packages/client/src/ui/screens/OnboardingScreen.tsx
    - packages/client/src/ui/App.tsx
    - packages/client/src/ui/theme.ts
  </read_first>
  <action>
    **SettingsOverlay 컴포넌트 생성** — UserList overlay 패턴을 따름:

    Props: `{ identity: Identity; visible: boolean; onClose: () => void; onIdentityChange: (identity: Identity) => void }`

    3개 서브화면으로 구성하는 state machine:
    - `'menu'`: 메인 메뉴 — 프로필 정보 표시 + 메뉴 항목 리스트
    - `'nickname'`: 닉네임 변경 — IMETextInput 사용 (NICKNAME_REGEX 검증)
    - `'ai-cli'`: AI CLI 변경 — AiCliSelector 재사용

    **메인 메뉴 (`'menu'`):**
    - 상단에 현재 프로필 표시: `닉네임#TAG | AI CLI: Claude Code`
    - 아래 메뉴 항목: `[닉네임 변경]`, `[AI CLI 변경]` — 화살표로 선택, Enter로 진입
    - Esc로 닫기

    **닉네임 변경 (`'nickname'`):**
    - IMETextInput으로 새 닉네임 입력
    - onSubmit에서 NICKNAME_REGEX 검증 → updateIdentity({nickname}) → onIdentityChange(updated) → 시스템 메시지는 Plan 02에서 처리 → subScreen을 'menu'로 복귀하지 않고 onClose() 호출하여 설정 화면 닫기
    - Esc로 메뉴 복귀

    **AI CLI 변경 (`'ai-cli'`):**
    - AiCliSelector 재사용 (onSelect 콜백)
    - onSelect에서 updateIdentity({aiCli}) → onIdentityChange(updated) → onClose()

    `useInput({ isActive: visible && subScreen === 'menu' })` 패턴으로 키 충돌 방지.

    **App.tsx 수정:**
    - `ChatScreen`에 `onIdentityChange` prop 전달: `<ChatScreen identity={identity} onIdentityChange={setIdentity} />`
    - ChatScreenProps에 `onIdentityChange?: (identity: Identity) => void` 추가는 Plan 02에서 처리 (ChatScreen 통합 시)
  </action>
  <verify>
    <automated>cd /Users/jamie/Jamie_Dev/cling_talk && npx tsc --noEmit --project packages/client/tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export function SettingsOverlay" packages/client/src/ui/components/SettingsOverlay.tsx
    - grep -q "onIdentityChange" packages/client/src/ui/components/SettingsOverlay.tsx
    - grep -q "updateIdentity" packages/client/src/ui/components/SettingsOverlay.tsx
    - grep -q "IMETextInput\|AiCliSelector" packages/client/src/ui/components/SettingsOverlay.tsx
    - grep -q "onIdentityChange" packages/client/src/ui/App.tsx
    - grep -q "useInput" packages/client/src/ui/components/SettingsOverlay.tsx
    - grep -q "isActive" packages/client/src/ui/components/SettingsOverlay.tsx
  </acceptance_criteria>
  <done>SettingsOverlay가 프로필 표시 + 닉네임 변경 + AI CLI 변경 서브화면을 갖추고, App이 onIdentityChange를 ChatScreen에 전달</done>
</task>

</tasks>

<verification>
- `npx vitest run packages/client/src/identity/IdentityManager.test.ts` — updateIdentity 테스트 통과
- `npx tsc --noEmit --project packages/client/tsconfig.json` — 타입 에러 없음
- SettingsOverlay.tsx에 useInput + isActive 패턴 사용 확인
- App.tsx에 onIdentityChange 콜백 전달 확인
</verification>

<success_criteria>
- updateIdentity()가 TAG를 유지하며 닉네임/aiCli 변경
- SettingsOverlay가 3개 서브화면(menu, nickname, ai-cli) 구현
- App → ChatScreen으로 onIdentityChange 콜백 전달
- TypeScript 컴파일 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/09-settings-command/09-01-SUMMARY.md`
</output>
