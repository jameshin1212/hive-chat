---
phase: 18-welcome-section
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/ui/screens/ChatScreen.tsx
  - packages/client/src/ui/App.tsx
  - packages/client/src/ui/components/AsciiBanner.tsx
autonomous: true
requirements: [WELC-01, WELC-02, WELC-03, WELC-04]

must_haves:
  truths:
    - "ChatScreen lobby 상태에서 시스템 메시지로 ASCII 배너가 표시된다"
    - "프로필 카드(닉네임#태그, AI CLI, 연결 상태)가 시스템 메시지로 표시된다"
    - "HiveChat 버전이 시스템 메시지로 표시되며 __APP_VERSION__과 일치한다"
    - "Tips 영역에 사용법 안내가 시스템 메시지로 표시된다"
    - "메시지 전송/수신 시 웰컴 메시지가 자연스럽게 스크롤 아웃된다"
    - "App.tsx의 WelcomeBack splash가 제거되고 즉시 ChatScreen으로 진입한다"
  artifacts:
    - path: "packages/client/src/ui/screens/ChatScreen.tsx"
      provides: "Welcome system messages in connected handler"
      contains: "addSystemMessage"
    - path: "packages/client/src/ui/App.tsx"
      provides: "Direct ChatScreen entry without splash"
    - path: "packages/client/src/ui/components/AsciiBanner.tsx"
      provides: "Exportable banner text generation function"
      exports: ["AsciiBanner", "getBannerText"]
  key_links:
    - from: "packages/client/src/ui/screens/ChatScreen.tsx"
      to: "packages/client/src/ui/components/AsciiBanner.tsx"
      via: "getBannerText import for system message"
      pattern: "getBannerText"
    - from: "packages/client/src/ui/screens/ChatScreen.tsx"
      to: "addSystemMessage"
      via: "connected handler adds welcome messages"
      pattern: "case 'connected'"
---

<objective>
ChatScreen lobby에서 연결 시 ASCII 배너, 프로필 카드, 버전, Tips를 시스템 메시지로 표시하고, App.tsx WelcomeBack splash를 제거한다.

Purpose: 사용자가 채팅 대기 중 빈 화면 대신 유용한 정보를 보고, 채팅이 시작되면 자연스럽게 스크롤 아웃된다.
Output: 수정된 ChatScreen.tsx, App.tsx, AsciiBanner.tsx
</objective>

<execution_context>
@/Users/jamie/Jamie_Dev/claude_chat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamie/Jamie_Dev/claude_chat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-welcome-section/18-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From packages/client/src/ui/components/AsciiBanner.tsx:
```typescript
// Current: only React component, no text-only export
import figlet from 'figlet';
let FIGLET_BANNER: string; // 'HIVECHAT' in Standard font
const PLAIN_BANNER = '=== HIVECHAT ===';

export function AsciiBanner({ breakpoint }: { breakpoint?: Breakpoint }) // React component
```

From packages/client/src/ui/screens/ChatScreen.tsx:
```typescript
// Line 85-94: addSystemMessage helper
const addSystemMessage = useCallback((content: string, kind?: 'transition') => {
  const sysMsg: ChatMessage = {
    id: nextMessageId(),
    from: { nickname: 'system', tag: '0000', aiCli: 'Claude Code', schemaVersion: 1 },
    content,
    timestamp: Date.now(),
    kind,
  };
  setMessages(prev => [...prev, sysMsg].slice(-MAX_MESSAGES));
}, []);

// Line 112-116: current connected handler (REPLACE TARGET)
case 'connected':
  addSystemMessage('Connected', 'transition');
  addSystemMessage(`HiveChat v${__APP_VERSION__}`);
  addSystemMessage('Tips: Tab — nearby users  |  /addfriend nick#TAG — add friend  |  /help — all commands');
  break;
```

From packages/client/src/ui/App.tsx:
```typescript
// Line 12-27: WelcomeBack splash (REMOVE TARGET)
const [showWelcomeBack, setShowWelcomeBack] = useState(!!identity);
if (showWelcomeBack && identity) {
  setTimeout(() => setShowWelcomeBack(false), 1500);
  // renders AsciiBanner + welcome message
}
```

From packages/client/src/hooks/useTerminalSize.ts:
```typescript
export type Breakpoint = 'compact' | 'standard' | 'wide';
export function getBreakpoint(columns: number): Breakpoint;
```

From packages/client/src/ui/theme.ts:
```typescript
export const theme = {
  badge: { 'Claude Code': '#B87FFF', 'Codex': '#10A37F', 'Gemini': '#4285F4', 'Cursor': '#00D4AA' },
  // ...
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AsciiBanner에 텍스트 전용 함수 추가 + ChatScreen connected 핸들러를 웰컴 시스템 메시지로 교체</name>
  <read_first>
    - packages/client/src/ui/components/AsciiBanner.tsx
    - packages/client/src/ui/screens/ChatScreen.tsx (line 85-125)
    - packages/client/src/hooks/useTerminalSize.ts
    - packages/client/src/ui/theme.ts
  </read_first>
  <files>
    packages/client/src/ui/components/AsciiBanner.tsx
    packages/client/src/ui/screens/ChatScreen.tsx
  </files>
  <action>
1. AsciiBanner.tsx에 `getBannerText(breakpoint: Breakpoint): string` 함수를 export 추가:
   - compact breakpoint이면 PLAIN_BANNER 반환
   - 그 외 FIGLET_BANNER 반환
   - 기존 AsciiBanner React 컴포넌트는 유지 (OnboardingScreen에서 사용 중)

2. ChatScreen.tsx의 connected 핸들러(line 112-116)를 다음 시스템 메시지 시퀀스로 교체:

   a) ASCII 배너 (시스템 메시지):
      - `getBannerText(breakpoint)`를 import하여 호출
      - addSystemMessage로 배너 텍스트 삽입 (kind 없음, 일반 시스템 메시지)
      - compact breakpoint에서는 PLAIN_BANNER 사용

   b) 프로필 카드 (시스템 메시지):
      - 내용: `{identity.nickname}#{identity.tag}  |  {identity.aiCli}  |  Connected`
      - addSystemMessage로 삽입 (kind 없음)

   c) 버전 (시스템 메시지):
      - `HiveChat v${__APP_VERSION__}`
      - addSystemMessage로 삽입

   d) Tips (시스템 메시지):
      - 여러 줄로 나눠서 표시:
        `Tips:`
        `  Tab        — nearby users`
        `  /addfriend — add friend (nick#TAG)`
        `  /friends   — friend list`
        `  /help      — all commands`
      - 하나의 addSystemMessage에 multiline string으로 삽입

   e) Connected 구분선 (transition):
      - addSystemMessage('Connected', 'transition')을 시퀀스 마지막에 배치하여 웰컴 콘텐츠와 이후 대화를 구분

   **주의**: getBannerText 호출 시 breakpoint를 ChatScreen의 useTerminalSize에서 가져옴 (이미 line 43에서 destructure됨). useEffect의 dependency array에 breakpoint 추가 불필요 — connected 이벤트는 한 번만 발생.
  </action>
  <verify>
    <automated>cd /Users/jamie/Jamie_Dev/hive_chat && npx tsc --noEmit -p packages/client/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - AsciiBanner.tsx에 getBannerText 함수가 export됨
    - ChatScreen.tsx connected 핸들러에 배너, 프로필 카드, 버전, Tips, Connected transition 5종 시스템 메시지가 순서대로 추가됨
    - identity.nickname, identity.tag, identity.aiCli가 프로필 카드 메시지에 포함됨
    - __APP_VERSION__이 버전 메시지에 사용됨
    - TypeScript 컴파일 에러 없음
  </acceptance_criteria>
  <done>
    ChatScreen lobby에서 연결 시 ASCII 배너 + 프로필 카드 + 버전 + Tips + Connected 구분선이 시스템 메시지로 표시된다
  </done>
</task>

<task type="auto">
  <name>Task 2: App.tsx WelcomeBack splash 제거 + 빌드 검증</name>
  <read_first>
    - packages/client/src/ui/App.tsx
  </read_first>
  <files>
    packages/client/src/ui/App.tsx
  </files>
  <action>
1. App.tsx에서 WelcomeBack splash 관련 코드 제거:
   - `showWelcomeBack` state 제거 (line 12)
   - `if (showWelcomeBack && identity)` 블록 전체 제거 (line 15-27)
   - AsciiBanner import 제거 (line 6) — App.tsx에서 더 이상 사용하지 않음
   - theme import 제거 (line 8) — App.tsx에서 더 이상 사용하지 않음
   - formatIdentityDisplay import는 유지 여부 확인 — 다른 곳에서 사용하지 않으면 제거

2. 결과적으로 App.tsx는:
   - identity 없으면 OnboardingScreen
   - identity 있으면 즉시 ChatScreen (splash 없이)

3. 빌드 검증:
   - `npx tsc --noEmit` 통과 확인
   - `npm run build -w packages/client` 통과 확인
  </action>
  <verify>
    <automated>cd /Users/jamie/Jamie_Dev/hive_chat && npx tsc --noEmit -p packages/client/tsconfig.json && npm run build -w packages/client 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx에 showWelcomeBack 관련 코드가 없음
    - App.tsx에 setTimeout이 없음
    - returning user가 즉시 ChatScreen으로 진입함
    - TypeScript 컴파일 + 빌드 성공
  </acceptance_criteria>
  <done>
    App.tsx WelcomeBack splash가 완전히 제거되고, returning user가 splash 없이 즉시 ChatScreen에 진입한다
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit -p packages/client/tsconfig.json` — 타입 에러 없음
2. `npm run build -w packages/client` — 빌드 성공
3. ChatScreen.tsx에 getBannerText import가 존재
4. ChatScreen.tsx connected 핸들러에 5종 시스템 메시지 (배너, 프로필, 버전, Tips, Connected transition)
5. App.tsx에 showWelcomeBack, setTimeout 없음
</verification>

<success_criteria>
- ChatScreen 연결 시 ASCII 배너 + 프로필 카드 + 버전 + Tips가 시스템 메시지로 표시
- 메시지가 쌓이면 웰컴 콘텐츠가 자연스럽게 스크롤 아웃
- App.tsx에서 WelcomeBack splash 완전 제거
- 빌드 성공
</success_criteria>

<output>
After completion, create `.planning/phases/18-welcome-section/18-01-SUMMARY.md`
</output>
