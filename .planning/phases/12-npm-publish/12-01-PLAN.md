---
phase: 12-npm-publish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/package.json
  - packages/client/tsdown.config.ts
autonomous: true
requirements:
  - PUB-02
  - PUB-03

must_haves:
  truths:
    - "package.json에 bin, files, repository, keywords, description, license, engines가 모두 설정되어 있다"
    - "npm pack 결과물이 1MB 미만이다"
    - "빌드 경고 없이 dist/index.mjs가 생성된다"
  artifacts:
    - path: "packages/client/package.json"
      provides: "완전한 npm publish용 메타데이터"
      contains: "\"version\": \"1.2.0\""
    - path: "packages/client/tsdown.config.ts"
      provides: "tsdown 번들 설정 (deprecated warning 제거)"
      contains: "neverBundle"
  key_links:
    - from: "packages/client/package.json"
      to: "packages/client/bin/hivechat.js"
      via: "bin field"
      pattern: "\"hivechat\": \"./bin/hivechat.js\""
    - from: "packages/client/package.json"
      to: "packages/client/dist/index.mjs"
      via: "files field"
      pattern: "\"dist\""
---

<objective>
hivechat 패키지의 메타데이터를 npm publish 기준에 맞게 완비하고 번들 크기를 최적화한다.

Purpose: `npx hivechat`으로 누구나 실행할 수 있도록 package.json 메타데이터와 빌드 설정을 정비
Output: publish-ready package.json + 최적화된 빌드 설정
</objective>

<execution_context>
@/Users/jamie/Jamie_Dev/claude_chat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamie/Jamie_Dev/claude_chat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- 현재 packages/client/package.json 구조 -->
From packages/client/package.json:
```json
{
  "name": "hivechat",
  "version": "0.1.0",
  "type": "module",
  "bin": { "hivechat": "./bin/hivechat.js" },
  "files": ["bin", "dist"],
  "scripts": {
    "build": "tsdown src/index.tsx --format esm --out-dir dist --external hyperswarm --external b4a ..."
  }
}
```

From packages/client/bin/hivechat.js:
```js
#!/usr/bin/env node
import('../dist/index.js');
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: package.json 메타데이터 완비 + 버전 1.2.0</name>
  <files>packages/client/package.json</files>
  <read_first>packages/client/package.json</read_first>
  <action>
packages/client/package.json을 수정한다:

1. version을 "1.2.0"으로 변경
2. 다음 필드 추가:
   - "description": "Chat with nearby developers right from your terminal"
   - "keywords": ["chat", "cli", "terminal", "p2p", "developer", "tui", "hyperswarm"]
   - "license": "MIT"
   - "repository": { "type": "git", "url": "https://github.com/jamie-chat/hivechat" }
   - "homepage": "https://github.com/jamie-chat/hivechat#readme"
   - "engines": { "node": ">=20" }
   - "author": "Jamie"

3. bin entry의 import 경로 확인: bin/hivechat.js에서 `import('../dist/index.js')` → dist/index.mjs와 일치하지 않으므로 bin/hivechat.js를 `import('../dist/index.mjs')`로 수정하거나 tsdown 출력을 .js로 변경. tsdown config에서 처리 (Task 2).

4. files 필드 확인: ["bin", "dist"]가 정확한지 검증 — npm pack --dry-run으로 확인

NOTE: repository URL은 placeholder — 실제 GitHub repo URL로 사용자가 나중에 변경 가능. description은 영문 (npm 글로벌 배포 대상).
  </action>
  <verify>
    <automated>cd /Users/jamie/Jamie_Dev/claude_chat && node -e "const p=JSON.parse(require('fs').readFileSync('packages/client/package.json','utf8')); const checks = [p.version==='1.2.0', p.description, p.keywords?.length>0, p.license==='MIT', p.repository?.url, p.engines?.node]; console.log(checks.every(Boolean) ? 'PASS' : 'FAIL: '+JSON.stringify(checks))"</automated>
  </verify>
  <acceptance_criteria>
    - grep "1.2.0" packages/client/package.json 결과 있음
    - grep "description" packages/client/package.json 결과 있음
    - grep "keywords" packages/client/package.json 결과 있음
    - grep "license" packages/client/package.json 결과 있음
    - grep "repository" packages/client/package.json 결과 있음
    - grep "engines" packages/client/package.json 결과 있음
  </acceptance_criteria>
  <done>package.json에 version 1.2.0, description, keywords, license, repository, engines, author 모두 설정됨</done>
</task>

<task type="auto">
  <name>Task 2: tsdown config 정리 + 빌드 검증 + pack 크기 확인</name>
  <files>packages/client/tsdown.config.ts, packages/client/package.json, packages/client/bin/hivechat.js</files>
  <read_first>packages/client/package.json, packages/client/bin/hivechat.js</read_first>
  <action>
1. packages/client/tsdown.config.ts 생성:
   - tsdown의 deprecated `external` 경고를 제거하기 위해 config 파일로 마이그레이션
   - format: esm, outDir: dist
   - entry: src/index.tsx
   - deps.neverBundle: ["hyperswarm", "b4a", "hypercore-crypto", "sodium-native", "sodium-universal", "hyperdht", "dht-rpc", "@hyperswarm/dht"]

2. packages/client/package.json의 build script를 `tsdown`으로 단순화 (config 파일에서 설정 읽음):
   - "build": "tsdown"

3. bin/hivechat.js의 import 경로를 실제 빌드 출력과 일치시킨다:
   - tsdown ESM 출력은 .mjs 확장자 → bin에서 `import('../dist/index.mjs')` 사용
   - 또는 tsdown config에서 출력 확장자를 .js로 설정 (더 간결)

4. 빌드 실행하여 경고 없이 완료되는지 확인: `npm -w packages/client run build`

5. `cd packages/client && npm pack --dry-run`으로 패키지 크기 확인 — 1MB 미만이어야 함
  </action>
  <verify>
    <automated>cd /Users/jamie/Jamie_Dev/claude_chat && npm -w packages/client run build 2>&1 | grep -v "WARN" | grep "Build complete" && cd packages/client && npm pack --dry-run 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - packages/client/tsdown.config.ts 파일 존재
    - grep "neverBundle" packages/client/tsdown.config.ts 결과 있음
    - npm -w packages/client run build 실행 시 "WARN" 메시지 없음
    - npm pack --dry-run 결과 package size가 1MB 미만
    - bin/hivechat.js의 import 경로가 실제 빌드 출력 파일명과 일치
  </acceptance_criteria>
  <done>tsdown config 파일로 마이그레이션 완료, 빌드 경고 제거, npm pack 크기 1MB 미만 확인</done>
</task>

</tasks>

<verification>
1. `npm -w packages/client run build` — 경고 없이 성공
2. `cd packages/client && npm pack --dry-run` — 3개 파일, 1MB 미만
3. package.json에 모든 필수 메타데이터 포함
</verification>

<success_criteria>
- package.json version 1.2.0, description, keywords, license, repository, engines 설정 완료
- tsdown.config.ts로 빌드 설정 마이그레이션 (deprecated 경고 제거)
- npm pack 결과물 1MB 미만
- bin entry point가 실제 빌드 출력과 경로 일치
</success_criteria>

<output>
After completion, create `.planning/phases/12-npm-publish/12-01-SUMMARY.md`
</output>
