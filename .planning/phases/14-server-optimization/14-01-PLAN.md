---
phase: 14-server-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/server/package.json
  - packages/server/src/PresenceManager.ts
  - packages/server/src/GeoLocationService.ts
  - packages/server/src/SignalingServer.ts
  - packages/server/src/__tests__/PresenceManager.test.ts
  - packages/server/src/__tests__/FriendStatus.test.ts
autonomous: true
requirements: [SOPT-02, SOPT-03]

must_haves:
  truths:
    - "getNearbyUsers가 geohash 기반 인덱스를 사용하여 전체 Map 순회 없이 반경 내 사용자를 반환한다"
    - "PresenceManager.register()가 geohashIndex와 userGeohash에 사용자를 추가한다"
    - "PresenceManager.unregister()가 geohashIndex와 userGeohash에서 사용자를 제거하고, 빈 Set은 삭제한다"
    - "notifyFriendSubscribers가 friendReverseIndex로 O(1) 조회한다"
    - "friendReverseIndex가 구독 등록/해제 시 동기화된다"
  artifacts:
    - path: "packages/server/src/PresenceManager.ts"
      provides: "Geohash spatial index (geohashIndex, userGeohash maps) + getUsersInRadius()"
      contains: "geohashIndex"
    - path: "packages/server/src/GeoLocationService.ts"
      provides: "getGeohashCells() 함수"
      contains: "getGeohashCells"
    - path: "packages/server/src/SignalingServer.ts"
      provides: "friendReverseIndex + notifyFriendSubscribers 역 인덱스 기반"
      contains: "friendReverseIndex"
  key_links:
    - from: "packages/server/src/PresenceManager.ts"
      to: "packages/server/src/GeoLocationService.ts"
      via: "getGeohashCells import"
      pattern: "import.*getGeohashCells.*from.*GeoLocationService"
    - from: "packages/server/src/PresenceManager.ts"
      to: "geohashing"
      via: "encodeBase32 for register"
      pattern: "encodeBase32"
    - from: "packages/server/src/SignalingServer.ts"
      to: "friendReverseIndex"
      via: "notifyFriendSubscribers uses reverse index"
      pattern: "friendReverseIndex\\.get"
---

<objective>
Geohash 공간 인덱스를 PresenceManager에 추가하고, 친구 알림을 역 인덱스 기반으로 전환한다.

Purpose: SOPT-02 (공간 인덱스)와 SOPT-03 (친구 역 인덱스) 구현. Plan 02의 지역 기반 broadcast를 위한 데이터 구조 기반 마련.
Output: geohash 인덱스가 추가된 PresenceManager, getUsersInRadius() 메서드, friendReverseIndex가 추가된 SignalingServer
</objective>

<execution_context>
@/Users/jamie/Jamie_Dev/hive_chat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamie/Jamie_Dev/hive_chat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-server-optimization/14-CONTEXT.md
@.planning/phases/14-server-optimization/14-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From packages/server/src/types.ts:
```typescript
export interface UserRecord {
  nickname: string;
  tag: string;
  aiCli: string;
  lat: number;
  lon: number;
  ws: WebSocket;
  lastHeartbeat: number;
  status: 'online' | 'offline';
}
```

From packages/server/src/GeoLocationService.ts:
```typescript
export function getDistanceKm(a: { lat: number; lon: number }, b: { lat: number; lon: number }): number;
export function findNearbyUsers(origin: { lat: number; lon: number }, users: Map<string, UserRecord>, radiusKm: number, excludeId?: string): Array<{ id: string; distance: number }>;
```

From packages/server/src/PresenceManager.ts:
```typescript
export class PresenceManager {
  register(id: string, record: UserRecord): void;
  unregister(id: string): void;
  getUser(id: string): UserRecord | undefined;
  heartbeat(id: string): void;
  checkStaleUsers(): string[];
  getNearbyUsers(originId: string, radiusKm: number): NearbyUser[];
  getAllOnlineUsers(): UserRecord[];
  getUserCount(): number;
}
```

From packages/server/src/SignalingServer.ts (friend-related):
```typescript
// L33: private friendSubscriptions = new Map<string, Array<{ nickname: string; tag: string }>>();
// L409-432: handleFriendStatusRequest(ws, friends) - stores subscription, returns current status
// L434-453: notifyFriendSubscribers(userId, status) - O(N*F) loop over all subscriptions
// L372: friendSubscriptions.delete(ws.userId) on disconnect
```

From packages/shared/src/constants.ts:
```typescript
export const DEFAULT_RADIUS_KM = 3;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Geohash 공간 인덱스 — GeoLocationService + PresenceManager</name>
  <files>
    packages/server/package.json,
    packages/server/src/GeoLocationService.ts,
    packages/server/src/PresenceManager.ts,
    packages/server/src/__tests__/PresenceManager.test.ts
  </files>
  <read_first>
    packages/server/src/GeoLocationService.ts,
    packages/server/src/PresenceManager.ts,
    packages/server/src/types.ts,
    packages/server/src/__tests__/PresenceManager.test.ts,
    packages/server/package.json
  </read_first>
  <behavior>
    - getGeohashCells(lat, lon)가 center + 8 neighbors = 9개 geohash 문자열 배열을 반환한다
    - PresenceManager.register() 후 geohashIndex에 해당 geohash 셀에 userId가 포함된다
    - PresenceManager.unregister() 후 geohashIndex에서 userId가 제거된다
    - unregister 후 빈 Set이면 해당 geohash 키 자체가 Map에서 삭제된다
    - getUsersInRadius(lat, lon, radiusKm, excludeId)가 9셀 조회 후 haversine 필터링으로 반경 내 사용자만 반환한다
    - getNearbyUsers(originId, radiusKm)가 기존과 동일한 NearbyUser[] 결과를 반환한다 (regression 없음)
  </behavior>
  <action>
    1. `cd packages/server && npm install geohashing` — pure JS, ESM, zero deps, TypeScript 네이티브

    2. GeoLocationService.ts에 `getGeohashCells` 함수 추가 (기존 함수는 모두 유지):
    ```typescript
    import { encodeBase32, getNeighborsBase32 } from 'geohashing';

    const GEOHASH_PRECISION = 5; // ~5km cells

    export function encodeGeohash(lat: number, lon: number): string {
      return encodeBase32(lat, lon, GEOHASH_PRECISION);
    }

    export function getGeohashCells(lat: number, lon: number): string[] {
      const center = encodeBase32(lat, lon, GEOHASH_PRECISION);
      const n = getNeighborsBase32(center);
      return [center, n.north, n.northEast, n.east, n.southEast, n.south, n.southWest, n.west, n.northWest];
    }
    ```

    3. PresenceManager.ts에 geohash 인덱스 추가:
    - private 필드 추가: `geohashIndex = new Map<string, Set<string>>()`, `userGeohash = new Map<string, string>()`
    - `import { encodeGeohash, getGeohashCells, getDistanceKm } from './GeoLocationService.js'`
    - `register()`: 기존 로직 유지 + geohash 계산 및 인덱스 추가. 기존 사용자가 있으면 `removeFromGeohashIndex(id)` 먼저 호출.
    - `unregister()`: `removeFromGeohashIndex(id)` 호출 후 `this.users.delete(id)`
    - private `removeFromGeohashIndex(id)`: userGeohash에서 hash 조회 -> geohashIndex.get(hash)에서 id 삭제 -> Set.size === 0이면 geohashIndex.delete(hash) -> userGeohash.delete(id)
    - 새 public 메서드 `getUsersInRadius(lat: number, lon: number, radiusKm: number, excludeId?: string): string[]`:
      - `getGeohashCells(lat, lon)`으로 9셀 획득
      - 각 셀의 userId Set 순회
      - excludeId 제외, status !== 'online' 제외
      - `getDistanceKm(origin, userPos) <= radiusKm`인 userId만 수집
      - userId[] 반환 (거리 정렬 불필요 — broadcast용)
    - `getNearbyUsers()` 리팩토링: 내부에서 `findNearbyUsers()` 대신 getUsersInRadius + getDistanceKm 조합으로 변경. 반환 형태는 기존 NearbyUser[] 동일 유지. 거리 기준 정렬 유지.

    4. findNearbyUsers()는 GeoLocationService.ts에 그대로 유지 (다른 곳에서 사용될 수 있으므로 삭제하지 않음). PresenceManager만 새 인덱스 기반으로 전환.

    5. 테스트 작성 (PresenceManager.test.ts 수정/확장):
    - `getGeohashCells` 단위 테스트: 서울 좌표(37.5665, 126.978)로 9개 셀 반환 확인
    - register -> getUsersInRadius: 같은 위치의 사용자 2명 등록, 10km 반경에서 조회 시 1명 반환
    - register -> unregister -> getUsersInRadius: 해제 후 조회 시 빈 배열
    - 먼 거리(부산 35.1796, 129.0756) 사용자는 10km 반경에 미포함
    - getNearbyUsers regression: 기존과 동일한 NearbyUser[] 형태 반환
  </action>
  <verify>
    <automated>cd /Users/jamie/Jamie_Dev/hive_chat && npx vitest run packages/server/src/__tests__/PresenceManager.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "geohashIndex" packages/server/src/PresenceManager.ts` >= 5
    - `grep -c "userGeohash" packages/server/src/PresenceManager.ts` >= 4
    - `grep "getUsersInRadius" packages/server/src/PresenceManager.ts` 존재
    - `grep "encodeGeohash" packages/server/src/GeoLocationService.ts` 존재
    - `grep "getGeohashCells" packages/server/src/GeoLocationService.ts` 존재
    - `grep "geohashing" packages/server/package.json` 존재
    - `grep "removeFromGeohashIndex" packages/server/src/PresenceManager.ts` 존재
    - `grep "set.size === 0\|set\.size === 0\|\.size === 0" packages/server/src/PresenceManager.ts` 존재 (빈 Set 정리)
    - vitest PresenceManager.test.ts 전체 통과
  </acceptance_criteria>
  <done>
    PresenceManager가 geohash 인덱스를 사용하여 register/unregister 시 인덱스를 동기화하고,
    getUsersInRadius()가 9셀 조회 + haversine 필터링으로 O(N) 없이 반경 내 사용자를 반환한다.
    getNearbyUsers()가 기존과 동일한 결과를 반환한다 (regression 없음).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Friend 역 인덱스 — notifyFriendSubscribers 최적화</name>
  <files>
    packages/server/src/SignalingServer.ts,
    packages/server/src/__tests__/FriendStatus.test.ts
  </files>
  <read_first>
    packages/server/src/SignalingServer.ts,
    packages/server/src/__tests__/FriendStatus.test.ts
  </read_first>
  <behavior>
    - handleFriendStatusRequest 호출 시 friendReverseIndex에 targetUserId -> subscriberId 매핑이 추가된다
    - 같은 subscriber가 다시 handleFriendStatusRequest 호출 시 기존 역 인덱스 항목이 정리되고 새로 등록된다
    - disconnect 시 해당 subscriber의 역 인덱스 항목이 모두 제거된다
    - notifyFriendSubscribers가 friendReverseIndex.get(userId)로 O(1) 조회한다
    - 기존 friend status 기능(온/오프라인 알림)이 동일하게 동작한다
  </behavior>
  <action>
    1. SignalingServer.ts에 역 인덱스 추가:
    - private 필드 추가: `private friendReverseIndex = new Map<string, Set<string>>()`
    - private 필드 추가: `private subscriberTargets = new Map<string, Set<string>>()` — subscriber -> Set<targetUserId> (cleanup용 정방향 매핑, RESEARCH.md Open Question 2 해결)

    2. private `updateFriendReverseIndex(subscriberId: string, friends: Array<{ nickname: string; tag: string }>)` 메서드 추가:
    ```typescript
    private updateFriendReverseIndex(subscriberId: string, friends: Array<{ nickname: string; tag: string }>): void {
      // 기존 역 인덱스에서 이 구독자 제거 (정방향 매핑 활용 — O(친구 수))
      this.cleanReverseIndexForSubscriber(subscriberId);

      // 새 친구 목록으로 역 인덱스 재구축
      const targets = new Set<string>();
      for (const friend of friends) {
        const targetId = `${friend.nickname}#${friend.tag}`;
        targets.add(targetId);
        if (!this.friendReverseIndex.has(targetId)) {
          this.friendReverseIndex.set(targetId, new Set());
        }
        this.friendReverseIndex.get(targetId)!.add(subscriberId);
      }
      this.subscriberTargets.set(subscriberId, targets);
    }
    ```

    3. private `cleanReverseIndexForSubscriber(subscriberId: string)` 메서드 추가:
    ```typescript
    private cleanReverseIndexForSubscriber(subscriberId: string): void {
      const targets = this.subscriberTargets.get(subscriberId);
      if (!targets) return;
      for (const targetId of targets) {
        const subs = this.friendReverseIndex.get(targetId);
        if (subs) {
          subs.delete(subscriberId);
          if (subs.size === 0) this.friendReverseIndex.delete(targetId);
        }
      }
      this.subscriberTargets.delete(subscriberId);
    }
    ```

    4. `handleFriendStatusRequest()` 수정 (L414 부근):
    - 기존 `this.friendSubscriptions.set(ws.userId!, friends)` 다음 줄에 추가:
    - `this.updateFriendReverseIndex(ws.userId!, friends);`

    5. `notifyFriendSubscribers()` 전체 교체 (L434-453):
    ```typescript
    private notifyFriendSubscribers(userId: string, status: 'online' | 'offline'): void {
      const lastHash = userId.lastIndexOf('#');
      if (lastHash === -1) return;
      const nickname = userId.substring(0, lastHash);
      const tag = userId.substring(lastHash + 1);

      const subscribers = this.friendReverseIndex.get(userId);
      if (!subscribers) return;

      for (const subscriberId of subscribers) {
        if (subscriberId === userId) continue;
        this.sendToUser(subscriberId, {
          type: MessageType.FRIEND_STATUS_UPDATE,
          nickname,
          tag,
          status,
        });
      }
    }
    ```

    6. `handleDisconnect()` 수정 (L372 부근):
    - 기존 `this.friendSubscriptions.delete(ws.userId)` 다음 줄에 추가:
    - `this.cleanReverseIndexForSubscriber(ws.userId);`

    7. FriendStatus.test.ts에 역 인덱스 동작 확인 테스트 추가:
    - 기존 테스트가 여전히 통과하는지 확인 (regression)
    - 새 테스트: 구독자 A가 friend B를 구독 -> B 온라인 -> A가 FRIEND_STATUS_UPDATE 수신
    - 새 테스트: 구독자 A disconnect 후 B 온라인 -> 아무도 알림 안 받음 (cleanup 확인)
  </action>
  <verify>
    <automated>cd /Users/jamie/Jamie_Dev/hive_chat && npx vitest run packages/server/src/__tests__/FriendStatus.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `grep "friendReverseIndex" packages/server/src/SignalingServer.ts` 존재
    - `grep "subscriberTargets" packages/server/src/SignalingServer.ts` 존재
    - `grep "updateFriendReverseIndex" packages/server/src/SignalingServer.ts` 존재
    - `grep "cleanReverseIndexForSubscriber" packages/server/src/SignalingServer.ts` 존재
    - notifyFriendSubscribers 내부에 `for (const \[subscriberId, friends\] of this.friendSubscriptions)` 패턴이 더 이상 없음
    - `grep "friendReverseIndex.get(userId)" packages/server/src/SignalingServer.ts` 존재
    - vitest FriendStatus.test.ts 전체 통과
  </acceptance_criteria>
  <done>
    notifyFriendSubscribers가 friendReverseIndex를 사용하여 O(1) 조회로 구독자를 찾고,
    subscriberTargets 정방향 매핑으로 cleanup이 O(친구 수)로 동작한다.
    기존 friend status 기능이 동일하게 동작한다 (regression 없음).
  </done>
</task>

</tasks>

<verification>
cd /Users/jamie/Jamie_Dev/hive_chat && npx vitest run packages/server --reporter=verbose
</verification>

<success_criteria>
- PresenceManager에 geohashIndex, userGeohash Map이 존재하고 register/unregister 시 동기화
- getUsersInRadius()가 geohash 9셀 조회 + haversine 필터링으로 동작
- getNearbyUsers()가 기존과 동일한 결과 반환
- SignalingServer에 friendReverseIndex, subscriberTargets Map이 존재
- notifyFriendSubscribers가 역 인덱스 기반 O(1) 조회로 동작
- 전체 서버 테스트 통과 (regression 없음)
</success_criteria>

<output>
After completion, create `.planning/phases/14-server-optimization/14-01-SUMMARY.md`
</output>
