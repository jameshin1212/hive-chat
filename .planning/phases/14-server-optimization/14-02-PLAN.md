---
phase: 14-server-optimization
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - packages/server/src/SignalingServer.ts
  - packages/server/src/__tests__/SignalingServer.test.ts
autonomous: true
requirements: [SOPT-01]

must_haves:
  truths:
    - "USER_JOINED broadcast가 10km 반경 내 사용자에게만 전송된다"
    - "USER_LEFT broadcast가 10km 반경 내 사용자에게만 전송된다"
    - "USER_STATUS (stale) broadcast가 10km 반경 내 사용자에게만 전송된다"
    - "10km 밖 사용자는 USER_JOINED/USER_LEFT/USER_STATUS를 수신하지 않는다"
    - "기존 사용자 발견, 채팅 요청 기능이 동일하게 동작한다"
  artifacts:
    - path: "packages/server/src/SignalingServer.ts"
      provides: "broadcastToNearby() 메서드, BROADCAST_RADIUS_KM 상수"
      contains: "broadcastToNearby"
    - path: "packages/server/src/__tests__/SignalingServer.test.ts"
      provides: "지역 기반 broadcast 테스트"
      contains: "broadcastToNearby"
  key_links:
    - from: "packages/server/src/SignalingServer.ts"
      to: "packages/server/src/PresenceManager.ts"
      via: "getUsersInRadius() 호출"
      pattern: "presenceManager\\.getUsersInRadius"
    - from: "SignalingServer.broadcastToNearby"
      to: "SignalingServer.sendToUser"
      via: "개별 사용자에게 메시지 전송"
      pattern: "sendToUser"
---

<objective>
broadcastToRegistered()를 지역 기반 broadcastToNearby()로 교체하여 USER_JOINED/USER_LEFT/USER_STATUS를 10km 반경 내 사용자에게만 전송한다.

Purpose: SOPT-01 구현. 전체 클라이언트 순회(O(N)) 대신 geohash 기반 지역 사용자에게만 broadcast하여 대규모 사용자 환경에서 효율적 동작.
Output: broadcastToNearby() 메서드 + 3곳의 호출부 교체 + 테스트
</objective>

<execution_context>
@/Users/jamie/Jamie_Dev/hive_chat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamie/Jamie_Dev/hive_chat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-server-optimization/14-CONTEXT.md
@.planning/phases/14-server-optimization/14-RESEARCH.md
@.planning/phases/14-server-optimization/14-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 생성된 인터페이스 — executor는 이 SUMMARY를 읽어 정확한 시그니처 확인 -->

From packages/server/src/PresenceManager.ts (Plan 01에서 추가):
```typescript
// 새 public 메서드
getUsersInRadius(lat: number, lon: number, radiusKm: number, excludeId?: string): string[];
// userId의 UserRecord 조회
getUser(id: string): UserRecord | undefined;
```

From packages/server/src/SignalingServer.ts (현재 상태, Plan 01 이후):
```typescript
// L491-500 — 교체 대상
private broadcastToRegistered(message: ServerMessage, excludeUserId?: string): void;

// 호출부 3곳:
// L193 (handleRegister): USER_JOINED broadcast
// L376 (handleDisconnect): USER_LEFT broadcast
// L474 (startHeartbeatCheck): USER_STATUS broadcast for stale users

// 기존 유틸
private sendToUser(targetUserId: string, message: ServerMessage): boolean;
private send(ws: WebSocket, message: ServerMessage): void;
```

From packages/shared/src/constants.ts:
```typescript
export const DEFAULT_RADIUS_KM = 3; // 클라이언트 검색용, broadcast와 무관
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: broadcastToNearby 구현 + 호출부 교체</name>
  <files>
    packages/server/src/SignalingServer.ts,
    packages/server/src/__tests__/SignalingServer.test.ts
  </files>
  <read_first>
    packages/server/src/SignalingServer.ts,
    packages/server/src/PresenceManager.ts,
    packages/server/src/__tests__/SignalingServer.test.ts,
    packages/shared/src/constants.ts
  </read_first>
  <behavior>
    - broadcastToNearby(message, originLat, originLon, radiusKm, excludeUserId)가 presenceManager.getUsersInRadius()를 호출하여 반경 내 사용자에게만 메시지 전송
    - handleRegister의 USER_JOINED broadcast가 등록자 위치 기준 10km 반경으로 전송
    - handleDisconnect의 USER_LEFT broadcast가 떠난 사용자 위치 기준 10km 반경으로 전송
    - startHeartbeatCheck의 USER_STATUS broadcast가 stale 사용자 위치 기준 10km 반경으로 전송
    - 같은 위치(서울)의 두 클라이언트: 서로 USER_JOINED 수신
    - 먼 위치(서울 vs 부산)의 두 클라이언트: 서로 USER_JOINED 미수신
  </behavior>
  <action>
    1. SignalingServer.ts 상단에 서버 전용 broadcast 반경 상수 추가:
    ```typescript
    /** Server broadcast radius - fixed at 10km per user decision */
    const BROADCAST_RADIUS_KM = 10;
    ```

    2. 새 private 메서드 `broadcastToNearby` 추가:
    ```typescript
    /**
     * Broadcast a message to users within radius of origin coordinates.
     * Uses geohash spatial index via PresenceManager.getUsersInRadius().
     */
    private broadcastToNearby(
      message: ServerMessage,
      originLat: number,
      originLon: number,
      radiusKm: number,
      excludeUserId?: string,
    ): void {
      const nearbyUserIds = this.presenceManager.getUsersInRadius(
        originLat, originLon, radiusKm, excludeUserId,
      );
      for (const userId of nearbyUserIds) {
        this.sendToUser(userId, message);
      }
    }
    ```

    3. handleRegister (L193 부근) — broadcastToRegistered 호출을 broadcastToNearby로 교체:
    ```typescript
    // 기존: this.broadcastToRegistered({ type: MessageType.USER_JOINED, ... }, userId);
    // 교체:
    this.broadcastToNearby(
      {
        type: MessageType.USER_JOINED,
        user: {
          nickname: msg.nickname,
          tag: msg.tag,
          aiCli: msg.aiCli as NearbyUser['aiCli'],
          distance: 0,
          status: 'online',
        },
      },
      geo.lat,
      geo.lon,
      BROADCAST_RADIUS_KM,
      userId,
    );
    ```
    주의: `geo` 변수는 handleRegister 스코프에 이미 존재함 (L160에서 lookupIp 결과).

    4. handleDisconnect (L376 부근) — broadcastToRegistered 호출을 broadcastToNearby로 교체:
    ```typescript
    // user 변수는 L350에서 이미 조회됨: const user = this.presenceManager.getUser(ws.userId);
    // 중요: unregister(L374) 전에 user 정보를 저장해야 함 — 현재 코드에서 user는 L350에서 이미 가져옴
    // 기존: this.broadcastToRegistered({ type: MessageType.USER_LEFT, ... }, ws.userId);
    // 교체:
    this.broadcastToNearby(
      {
        type: MessageType.USER_LEFT,
        nickname: user.nickname,
        tag: user.tag,
      },
      user.lat,
      user.lon,
      BROADCAST_RADIUS_KM,
      ws.userId,
    );
    ```
    주의: broadcastToNearby 호출은 presenceManager.unregister(L374) 이후이므로, getUsersInRadius에서 이 사용자는 이미 제외됨. excludeUserId도 전달하지만 방어적으로 유지.

    5. startHeartbeatCheck (L474 부근) — broadcastToRegistered 호출을 broadcastToNearby로 교체:
    ```typescript
    // 기존: this.broadcastToRegistered({ type: MessageType.USER_STATUS, ... }, undefined);
    // 교체:
    this.broadcastToNearby(
      {
        type: MessageType.USER_STATUS,
        nickname: user.nickname,
        tag: user.tag,
        status: 'offline',
      },
      user.lat,
      user.lon,
      BROADCAST_RADIUS_KM,
    );
    ```

    6. 기존 `broadcastToRegistered` 메서드 삭제 (L491-500). 모든 호출부가 broadcastToNearby로 교체되었으므로 불필요.

    7. SignalingServer.test.ts에 지역 기반 broadcast 테스트 추가:
    - 테스트 환경: DEV_GEO_LAT/DEV_GEO_LON을 사용하여 위치 설정 (기존 패턴)
    - 테스트 1: 같은 위치(서울)에서 두 클라이언트 등록 -> 두 번째 등록 시 첫 번째가 USER_JOINED 수신
    - 테스트 2 (가능하면): 서로 다른 DEV_GEO 좌표로 두 클라이언트를 등록하기 어려울 수 있음 — 이 경우 같은 위치에서의 정상 동작만 확인하고, 먼 거리 테스트는 PresenceManager.test.ts의 getUsersInRadius 테스트로 커버
    - 기존 테스트가 모두 통과하는지 확인 (regression)
  </action>
  <verify>
    <automated>cd /Users/jamie/Jamie_Dev/hive_chat && npx vitest run packages/server --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `grep "BROADCAST_RADIUS_KM" packages/server/src/SignalingServer.ts` 존재
    - `grep "broadcastToNearby" packages/server/src/SignalingServer.ts` 존재
    - `grep "broadcastToRegistered" packages/server/src/SignalingServer.ts`가 0건 (메서드 완전 제거)
    - `grep "presenceManager.getUsersInRadius" packages/server/src/SignalingServer.ts` 존재
    - `grep "BROADCAST_RADIUS_KM = 10" packages/server/src/SignalingServer.ts` 존재 (10km 고정)
    - vitest packages/server 전체 통과
  </acceptance_criteria>
  <done>
    broadcastToRegistered()가 broadcastToNearby()로 완전히 교체되어,
    USER_JOINED/USER_LEFT/USER_STATUS가 발신자 위치 기준 10km 반경 내 사용자에게만 전송된다.
    기존 기능(사용자 발견, 채팅 요청, 친구 알림)이 모두 정상 동작한다.
  </done>
</task>

</tasks>

<verification>
cd /Users/jamie/Jamie_Dev/hive_chat && npx vitest run --reporter=verbose
</verification>

<success_criteria>
- broadcastToRegistered()가 완전히 제거되고 broadcastToNearby()로 교체
- 3곳의 호출부(handleRegister, handleDisconnect, startHeartbeatCheck)가 모두 broadcastToNearby 사용
- BROADCAST_RADIUS_KM = 10 상수가 서버에 정의
- 전체 테스트 스위트 통과 (regression 없음)
</success_criteria>

<output>
After completion, create `.planning/phases/14-server-optimization/14-02-SUMMARY.md`
</output>
