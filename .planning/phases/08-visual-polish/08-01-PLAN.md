---
phase: 08-visual-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/ui/components/MessageArea.tsx
autonomous: true
requirements:
  - VIS-02
  - VIS-03

must_haves:
  truths:
    - "system message appears as gray italic text without badge/nickname/timestamp"
    - "own messages have green text color on content"
    - "other users' messages have default white text color on content"
  artifacts:
    - path: "packages/client/src/ui/components/MessageArea.tsx"
      provides: "system message branch rendering + own message color"
      contains: "from.nickname === 'system'"
  key_links:
    - from: "MessageArea.tsx"
      to: "theme.ts"
      via: "theme.text.primary for own message, theme.text.secondary for system"
      pattern: "theme\\.text\\.(primary|secondary)"
---

<objective>
MessageArea 메시지 렌더링 시각 구분 — 시스템 메시지 스타일 분리 및 내 메시지 색상 구분

Purpose: 시스템 메시지(입장/퇴장/연결 상태)가 사용자 채팅과 섞여 구분이 어려운 문제 해결, 내 메시지와 상대방 메시지 색상 구분
Output: MessageArea.tsx에 시스템 메시지 분기 렌더링 + 메시지 본문 색상 분기
</objective>

<execution_context>
@/Users/jamie/Jamie_Dev/claude_chat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamie/Jamie_Dev/claude_chat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-visual-polish/08-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From packages/client/src/ui/theme.ts:
```typescript
export const theme = {
  text: {
    primary: THEME_COLORS.primary,     // green
    secondary: 'gray',
    error: 'red',
    info: 'cyan',
  },
  // ...
} as const;
```

From packages/shared (ChatMessage type):
```typescript
interface ChatMessage {
  id: string;
  from: Identity;  // { nickname: string, tag: string, aiCli: string, schemaVersion: number }
  content: string;
  timestamp: number;
}
```

System messages use: `from.nickname === 'system'` (set in ChatScreen.tsx addSystemMessage)
Own message check: `msg.from.nickname === myIdentity.nickname && msg.from.tag === myIdentity.tag`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: System message styling + message color differentiation</name>
  <files>packages/client/src/ui/components/MessageArea.tsx</files>
  <read_first>
    - packages/client/src/ui/components/MessageArea.tsx
    - packages/client/src/ui/theme.ts
    - packages/client/src/ui/screens/ChatScreen.tsx (L62-70 for addSystemMessage pattern)
  </read_first>
  <action>
MessageArea.tsx의 visibleMessages.map 렌더링 루프(L97-116)에서 시스템 메시지와 일반 메시지 분기 처리:

**시스템 메시지 분기** (VIS-02):
- 조건: `msg.from.nickname === 'system'`
- 렌더링: 배지, 닉네임, 타임스탬프 모두 제거. 구분선 문자로 감싼 회색 이탤릭 텍스트만 표시
- 형식: `<Text color={theme.text.secondary} italic>{'\u2500'} {msg.content} {'\u2500'}</Text>`
- 기존 `[timestamp] [aiCli] nickname#tag: content` 형식 대신 위 형식 사용

**메시지 본문 색상 분기** (VIS-03):
- 일반 메시지(시스템 아닌)에서 `isOwnMessage` 판별 (기존 L103-105 로직 활용)
- 내 메시지 content: `<Text color={theme.text.primary}>{msg.content}</Text>` (green)
- 상대방 메시지 content: `<Text>{msg.content}</Text>` (기본 흰색, 현재와 동일)
- 닉네임 색상은 기존 로직 유지 (내 것=green, 상대방=USER_COLORS 순환)

분기 구조:
```
if (msg.from.nickname === 'system') {
  // render system style
} else {
  // render normal message with content color based on isOwnMessage
}
```
  </action>
  <verify>
    <automated>cd /Users/jamie/Jamie_Dev/cling_talk && npx tsc --noEmit -p packages/client/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "from.nickname === 'system'" packages/client/src/ui/components/MessageArea.tsx
    - grep -q "italic" packages/client/src/ui/components/MessageArea.tsx
    - grep -q "theme.text.secondary" packages/client/src/ui/components/MessageArea.tsx
    - grep -q "theme.text.primary" packages/client/src/ui/components/MessageArea.tsx
    - grep -q "isOwnMessage" packages/client/src/ui/components/MessageArea.tsx
    - grep -q "msg.content" packages/client/src/ui/components/MessageArea.tsx
  </acceptance_criteria>
  <done>
    - system 메시지가 `─ content ─` 형식의 회색 이탤릭으로 렌더링된다
    - 내 메시지 content가 green 색상으로 표시된다
    - 상대방 메시지 content는 기본 흰색으로 표시된다
    - TypeScript 컴파일 에러 없음
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p packages/client/tsconfig.json` passes
- MessageArea.tsx에 system 메시지 분기 렌더링 존재
- MessageArea.tsx에 isOwnMessage 기반 content 색상 분기 존재
</verification>

<success_criteria>
- 시스템 메시지가 badge/nickname/timestamp 없이 회색 이탤릭으로 렌더링
- 내 메시지 본문이 green, 상대방 메시지 본문이 흰색
- TypeScript 컴파일 성공
</success_criteria>

<output>
After completion, create `.planning/phases/08-visual-polish/08-01-SUMMARY.md`
</output>
