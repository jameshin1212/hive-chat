---
phase: 08-visual-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/commands/CommandParser.ts
  - packages/client/src/ui/screens/ChatScreen.tsx
autonomous: true
requirements:
  - VIS-01
  - CMD-01

must_haves:
  truths:
    - "StatusBar is positioned directly above the input field (between message area and input)"
    - "/exit command exits the application"
    - "/quit is no longer a recognized command"
    - "/help output shows /exit instead of /quit"
  artifacts:
    - path: "packages/client/src/commands/CommandParser.ts"
      provides: "/exit command definition (replacing /quit)"
      contains: "'/exit'"
    - path: "packages/client/src/ui/screens/ChatScreen.tsx"
      provides: "StatusBar relocated below MessageArea, /exit handler"
      contains: "parsed.name === '/exit'"
  key_links:
    - from: "ChatScreen.tsx"
      to: "CommandParser.ts"
      via: "parseInput + COMMANDS import"
      pattern: "parsed\\.name === '/exit'"
    - from: "ChatScreen.tsx JSX"
      to: "StatusBar component"
      via: "JSX render order: MessageArea → StatusBar → Input"
      pattern: "StatusBar"
---

<objective>
상태바를 입력 필드 바로 위로 재배치하고, /quit 명령어를 /exit로 변경

Purpose: 상태바가 메시지 영역 아래(입력 근처)에 위치하여 현재 상태를 더 쉽게 확인, /exit가 더 직관적인 종료 명령어
Output: ChatScreen.tsx 레이아웃 변경 + CommandParser.ts 명령어 변경
</objective>

<execution_context>
@/Users/jamie/Jamie_Dev/claude_chat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamie/Jamie_Dev/claude_chat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-visual-polish/08-CONTEXT.md

<interfaces>
<!-- Current ChatScreen.tsx layout order (L268-339): -->
<!-- StatusBar → separator → MessageArea/UserList/FriendList → CommandSuggestions → separator → IMETextInput -->

<!-- Target layout order (from CONTEXT.md decisions): -->
<!-- MessageArea/UserList/FriendList → separator → StatusBar → separator → IMETextInput -->
<!-- CommandSuggestions stays above input = above StatusBar -->

From packages/client/src/commands/CommandParser.ts:
```typescript
export const COMMANDS = {
  '/quit': { description: 'Exit Cling Talk' },
  '/users': { description: 'Show nearby users' },
  // ... other commands
} as const;
```

ChatScreen.tsx L150: `if (parsed.name === '/quit') { gracefulExit(); }`
ChatScreen.tsx L42: `const messageAreaHeight = rows - 4;`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: /quit to /exit command rename</name>
  <files>packages/client/src/commands/CommandParser.ts, packages/client/src/ui/screens/ChatScreen.tsx</files>
  <read_first>
    - packages/client/src/commands/CommandParser.ts
    - packages/client/src/ui/screens/ChatScreen.tsx (L146-160 for /quit handler)
  </read_first>
  <action>
**CommandParser.ts:**
- COMMANDS 객체에서 `'/quit'` 키를 `'/exit'`로 변경
- description은 동일하게 유지: `'Exit Cling Talk'`

**ChatScreen.tsx:**
- L150: `parsed.name === '/quit'` → `parsed.name === '/exit'`로 변경
- 다른 부분은 변경 없음 (parseInput, isKnownCommand 등은 COMMANDS 객체 기반이라 자동 반영)
  </action>
  <verify>
    <automated>cd /Users/jamie/Jamie_Dev/cling_talk && npx tsc --noEmit -p packages/client/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "'/exit'" packages/client/src/commands/CommandParser.ts
    - grep -qv "'/quit'" packages/client/src/commands/CommandParser.ts
    - grep -q "parsed.name === '/exit'" packages/client/src/ui/screens/ChatScreen.tsx
    - grep -qv "parsed.name === '/quit'" packages/client/src/ui/screens/ChatScreen.tsx
  </acceptance_criteria>
  <done>
    - /exit 명령어로 앱 종료 가능
    - /quit은 더 이상 인식되지 않음 (unknown command 응답)
    - /help에서 /exit 표시
  </done>
</task>

<task type="auto">
  <name>Task 2: StatusBar relocation below MessageArea</name>
  <files>packages/client/src/ui/screens/ChatScreen.tsx</files>
  <read_first>
    - packages/client/src/ui/screens/ChatScreen.tsx (L268-339 for JSX layout)
  </read_first>
  <action>
ChatScreen.tsx의 JSX 렌더 순서를 변경:

**현재 순서:**
```
StatusBar → separator → MessageArea → ChatRequestOverlay → CommandSuggestions → separator → IMETextInput
```

**변경 후 순서:**
```
MessageArea → CommandSuggestions → separator → StatusBar → separator → IMETextInput
```

구체적 변경:
1. `<StatusBar ... />` 블록(L270-279)을 MessageArea 뒤, 두 번째 separator 앞으로 이동
2. 첫 번째 separator(L280-282)를 StatusBar 바로 위로 이동 (MessageArea 아래)
3. CommandSuggestions(L322-328)를 첫 번째 separator 위로 이동 (MessageArea 바로 아래)
4. ChatRequestOverlay는 현재 위치 유지 (overlay이므로 순서 무관)

**최종 JSX 구조:**
```jsx
<Box flexDirection="column" height={rows}>
  {/* Message area / lists */}
  {showFriendList && !showUserList && !incomingRequest ? (
    <FriendList ... />
  ) : showUserList ? (
    <UserList ... />
  ) : (
    <MessageArea ... />
  )}
  {incomingRequest && <ChatRequestOverlay ... />}
  {showSuggestions && <CommandSuggestions ... />}
  <Box><Text color={theme.ui.separator}>{separator}</Text></Box>
  <StatusBar ... />
  <Box><Text color={theme.ui.separator}>{separator}</Text></Box>
  <IMETextInput ... />
</Box>
```

**messageAreaHeight 계산은 변경 없음** — 여전히 `rows - 4` (StatusBar 1줄 + separator 2줄 + Input 1줄 = 4줄 차감).
  </action>
  <verify>
    <automated>cd /Users/jamie/Jamie_Dev/cling_talk && npx tsc --noEmit -p packages/client/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "StatusBar" packages/client/src/ui/screens/ChatScreen.tsx | tail -1 | grep -q "[3-9][0-9][0-9]\|[2-9][0-9]"
    - grep -n "IMETextInput" packages/client/src/ui/screens/ChatScreen.tsx | tail -1
    - grep -q "messageAreaHeight = rows - 4" packages/client/src/ui/screens/ChatScreen.tsx
  </acceptance_criteria>
  <done>
    - StatusBar가 MessageArea 아래, IMETextInput 위에 위치
    - separator 2개가 StatusBar 위아래에 존재
    - CommandSuggestions가 StatusBar 위(MessageArea 바로 아래)에 표시
    - messageAreaHeight 계산이 정상 (rows - 4)
    - TypeScript 컴파일 에러 없음
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit -p packages/client/tsconfig.json` passes
- ChatScreen.tsx JSX에서 StatusBar가 MessageArea 뒤, IMETextInput 앞에 위치
- CommandParser.ts에 `/exit` 명령어 존재, `/quit` 없음
</verification>

<success_criteria>
- 상태바가 입력 필드 바로 윗 라인에 위치
- /exit 명령어로 앱 종료 가능
- /quit은 unknown command로 처리
- TypeScript 컴파일 성공
</success_criteria>

<output>
After completion, create `.planning/phases/08-visual-polish/08-02-SUMMARY.md`
</output>
