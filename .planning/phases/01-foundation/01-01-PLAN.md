---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - vitest.config.ts
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/src/types.ts
  - packages/shared/src/schemas.ts
  - packages/shared/src/constants.ts
  - packages/client/package.json
  - packages/client/tsconfig.json
  - packages/client/bin/cling-talk.js
  - packages/client/src/index.tsx
  - packages/client/src/identity/IdentityManager.ts
  - packages/client/src/identity/IdentityManager.test.ts
  - packages/client/src/config/AppConfig.ts
  - packages/client/src/commands/CommandParser.ts
  - packages/client/src/commands/CommandParser.test.ts
  - packages/client/src/ui/theme.ts
  - .gitignore
autonomous: true
requirements:
  - IDEN-01
  - IDEN-02
  - TUI-04
  - DIST-01

must_haves:
  truths:
    - "nick#XXXX identity is generated with crypto.randomBytes and persisted via conf"
    - "AI CLI selection is stored alongside identity"
    - "Slash commands /quit, /help are parsed correctly"
    - "npx cling-talk bin entry exists with correct shebang"
    - "All packages are ESM-only with strict TypeScript"
  artifacts:
    - path: "packages/shared/src/types.ts"
      provides: "Identity, AiCli, ChatMessage, SlashCommand types"
      exports: ["Identity", "AiCli", "AI_CLI_OPTIONS", "ChatMessage"]
    - path: "packages/shared/src/schemas.ts"
      provides: "Zod schemas for identity and config"
      exports: ["identitySchema", "configSchema"]
    - path: "packages/shared/src/constants.ts"
      provides: "Nickname rules, tag length, color definitions"
      exports: ["NICKNAME_REGEX", "TAG_LENGTH", "THEME_COLORS", "MAX_MESSAGES"]
    - path: "packages/client/src/identity/IdentityManager.ts"
      provides: "Identity CRUD operations"
      exports: ["IdentityManager", "generateTag", "loadIdentity", "saveIdentity"]
    - path: "packages/client/src/commands/CommandParser.ts"
      provides: "Slash command parsing"
      exports: ["parseInput", "COMMANDS"]
    - path: "packages/client/src/ui/theme.ts"
      provides: "Color theme constants"
      exports: ["theme", "USER_COLORS"]
  key_links:
    - from: "packages/client/src/identity/IdentityManager.ts"
      to: "packages/shared/src/schemas.ts"
      via: "import identitySchema for validation"
      pattern: "import.*from.*@cling-talk/shared"
    - from: "packages/client/src/identity/IdentityManager.ts"
      to: "conf"
      via: "Conf instance for persistent storage"
      pattern: "new Conf"
---

<objective>
Monorepo scaffolding, shared types, identity system, command parser, theme, and npm distribution entry point.

Purpose: Establish the project foundation that all subsequent TUI and onboarding work builds upon. Validate that the monorepo structure, TypeScript strict mode, and ESM-only configuration work end-to-end.
Output: Working monorepo with shared types, identity CRUD (nick#tag generation + AI CLI storage), slash command parser, color theme, and npx bin entry.
</objective>

<execution_context>
@/Users/jamie/Jamie_Dev/claude_chat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamie/Jamie_Dev/claude_chat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
@CLAUDE.md
@.claude/rules/monorepo-conventions.md
@.claude/rules/tui-cjk-input.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Monorepo scaffolding + shared types + identity system</name>
  <files>
    package.json, tsconfig.json, vitest.config.ts,
    packages/shared/package.json, packages/shared/tsconfig.json,
    packages/shared/src/types.ts, packages/shared/src/schemas.ts, packages/shared/src/constants.ts,
    packages/client/package.json, packages/client/tsconfig.json,
    packages/client/src/identity/IdentityManager.ts,
    packages/client/src/identity/IdentityManager.test.ts,
    packages/client/src/config/AppConfig.ts
  </files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md
    .claude/rules/monorepo-conventions.md
    CLAUDE.md
    .gitignore
  </read_first>
  <behavior>
    - generateTag() returns 4-char uppercase hex string (e.g. "3A7F")
    - saveIdentity("coder", "Claude Code") stores identity with schemaVersion: 1
    - loadIdentity() returns stored identity or undefined
    - nickname validation: accepts "coder", "my-nick", "a1_b2"; rejects "", "ab cd", "a".repeat(17), "UPPER"
    - identitySchema.parse() validates correct shape, rejects invalid
    - AI_CLI_OPTIONS contains exactly ["Claude Code", "Codex", "Gemini", "Cursor"]
  </behavior>
  <action>
    1. Create root `package.json`:
       - `"name": "cling-talk-monorepo"`, `"private": true`
       - `"type": "module"`
       - `"workspaces": ["packages/*"]`
       - `"engines": {"node": ">=20"}"`
       - scripts: `"test": "vitest run"`, `"build": "npm -w packages/client run build"`

    2. Create root `tsconfig.json`:
       - `"compilerOptions": {"strict": true, "target": "ES2022", "module": "Node16", "moduleResolution": "Node16", "noUncheckedIndexedAccess": true, "jsx": "react-jsx", "jsxImportSource": "react", "declaration": true, "esModuleInterop": true, "skipLibCheck": true}`
       - `"references": [{"path": "packages/shared"}, {"path": "packages/client"}]`

    3. Create root `vitest.config.ts`:
       ```typescript
       import { defineConfig } from 'vitest/config';
       export default defineConfig({
         test: {
           include: ['packages/*/src/**/*.test.{ts,tsx}'],
         },
       });
       ```

    4. Create `packages/shared/package.json`:
       - `"name": "@cling-talk/shared"`, `"type": "module"`, `"version": "0.1.0"`
       - `"exports": {".":{  "import": "./src/index.ts" }}`
       - `"files": ["src"]`

    5. Create `packages/shared/tsconfig.json`:
       - extends root, `"rootDir": "src"`, `"outDir": "dist"`

    6. Create `packages/shared/src/types.ts`:
       ```typescript
       export const AI_CLI_OPTIONS = ['Claude Code', 'Codex', 'Gemini', 'Cursor'] as const;
       export type AiCli = (typeof AI_CLI_OPTIONS)[number];

       export interface Identity {
         nickname: string;
         tag: string;        // 4-char uppercase hex, e.g. "3A7F"
         aiCli: AiCli;
         schemaVersion: 1;
       }

       export interface ChatMessage {
         id: string;
         from: Identity;
         content: string;
         timestamp: number;
       }

       export type SlashCommand = {
         type: 'command';
         name: string;
         args: string[];
       };

       export type ParsedInput =
         | SlashCommand
         | { type: 'message'; content: string };
       ```

    7. Create `packages/shared/src/schemas.ts`:
       ```typescript
       import { z } from 'zod';
       import { AI_CLI_OPTIONS } from './types.js';

       export const identitySchema = z.object({
         nickname: z.string().regex(/^[a-z0-9_-]{1,16}$/),
         tag: z.string().regex(/^[0-9A-F]{4}$/),
         aiCli: z.enum(AI_CLI_OPTIONS),
         schemaVersion: z.literal(1),
       });

       export const configSchema = z.object({
         identity: identitySchema.optional(),
       });
       ```

    8. Create `packages/shared/src/constants.ts`:
       ```typescript
       export const NICKNAME_REGEX = /^[a-z0-9_-]{1,16}$/;
       export const TAG_LENGTH = 4;
       export const MAX_MESSAGES = 500;
       export const MIN_TERMINAL_WIDTH = 60;
       export const DEFAULT_TERMINAL_WIDTH = 80;

       export const THEME_COLORS = {
         primary: 'green',
         separator: 'green',
         statusBar: 'green',
       } as const;

       export const USER_COLORS = [
         'white', 'yellow', 'red', '#FF69B4', '#FF1493', 'magenta', '#FFA500',
       ] as const;
       ```

    9. Create `packages/shared/src/index.ts` re-exporting all:
       ```typescript
       export * from './types.js';
       export * from './schemas.js';
       export * from './constants.js';
       ```

    10. Create `packages/client/package.json`:
        - `"name": "cling-talk"`, `"version": "0.1.0"`, `"type": "module"`
        - `"bin": {"cling-talk": "./bin/cling-talk.js"}`
        - `"files": ["bin", "dist"]`
        - dependencies: `ink@^6.8.0`, `react@^18`, `string-width@^8.2.0`, `conf@^15.1.0`, `zod@^3.24`, `chalk@^5.6.2`, `nanoid@^5.1.7`, `figlet@^1.11.0`, `@inkjs/ui@^2.0.0`, `@cling-talk/shared: "workspace:*"`
        - devDependencies: `typescript@^5.7`, `@types/react`, `tsdown`, `vitest`, `@types/figlet`
        - scripts: `"build": "tsdown src/index.tsx --format esm --out-dir dist"`, `"test": "vitest run"`

    11. Create `packages/client/tsconfig.json`:
        - extends root, `"rootDir": "src"`, `"outDir": "dist"`, references to shared

    12. Create `packages/client/src/config/AppConfig.ts`:
        ```typescript
        import Conf from 'conf';
        import type { Identity } from '@cling-talk/shared';

        export const appConfig = new Conf<{ identity?: Identity }>({
          projectName: 'cling-talk',
        });
        ```

    13. Create `packages/client/src/identity/IdentityManager.ts`:
        ```typescript
        import crypto from 'node:crypto';
        import { identitySchema, type Identity, type AiCli } from '@cling-talk/shared';
        import { appConfig } from '../config/AppConfig.js';

        export function generateTag(): string {
          return crypto.randomBytes(2).toString('hex').toUpperCase();
        }

        export function saveIdentity(nickname: string, aiCli: AiCli): Identity {
          const identity: Identity = {
            nickname,
            tag: generateTag(),
            aiCli,
            schemaVersion: 1,
          };
          identitySchema.parse(identity);
          appConfig.set('identity', identity);
          return identity;
        }

        export function loadIdentity(): Identity | undefined {
          const raw = appConfig.get('identity');
          if (!raw) return undefined;
          return identitySchema.parse(raw);
        }

        export function clearIdentity(): void {
          appConfig.delete('identity');
        }

        export function formatIdentityDisplay(identity: Identity): string {
          return `${identity.nickname}#${identity.tag}`;
        }
        ```

    14. Create `packages/client/src/identity/IdentityManager.test.ts`:
        - Test generateTag returns 4-char uppercase hex matching /^[0-9A-F]{4}$/
        - Test saveIdentity stores and returns valid Identity
        - Test loadIdentity returns undefined when no identity
        - Test saveIdentity -> loadIdentity round-trip
        - Test nickname validation rejects invalid patterns
        - Test formatIdentityDisplay returns "nick#TAG" format
        - Use `clearIdentity()` in afterEach for cleanup

    15. Install all dependencies: `npm install` from root

    16. Run `npx vitest run` to confirm tests pass
  </action>
  <verify>
    <automated>cd /Users/jamie/Jamie_Dev/claude_chat && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"workspaces": ["packages/*"]` and `"type": "module"`
    - `packages/shared/src/types.ts` contains `export const AI_CLI_OPTIONS = ['Claude Code', 'Codex', 'Gemini', 'Cursor'] as const`
    - `packages/shared/src/types.ts` contains `export interface Identity`
    - `packages/shared/src/schemas.ts` contains `export const identitySchema = z.object`
    - `packages/shared/src/constants.ts` contains `export const NICKNAME_REGEX`
    - `packages/client/src/identity/IdentityManager.ts` contains `crypto.randomBytes(2).toString('hex').toUpperCase()`
    - `packages/client/src/identity/IdentityManager.ts` contains `import { identitySchema`
    - `packages/client/src/identity/IdentityManager.test.ts` exits 0 via vitest
    - `packages/client/package.json` contains `"bin": {"cling-talk": "./bin/cling-talk.js"}`
    - `npx vitest run` exits 0 with all tests passing
  </acceptance_criteria>
  <done>
    Monorepo builds, shared types exported, identity CRUD works with tests passing, npm package.json has correct bin field.
  </done>
</task>

<task type="auto">
  <name>Task 2: Command parser + color theme + bin entry</name>
  <files>
    packages/client/src/commands/CommandParser.ts,
    packages/client/src/commands/CommandParser.test.ts,
    packages/client/src/ui/theme.ts,
    packages/client/bin/cling-talk.js,
    packages/client/src/index.tsx
  </files>
  <read_first>
    packages/shared/src/types.ts
    packages/shared/src/constants.ts
    packages/client/package.json
    .planning/phases/01-foundation/01-CONTEXT.md
  </read_first>
  <action>
    1. Create `packages/client/src/commands/CommandParser.ts`:
       ```typescript
       import type { ParsedInput } from '@cling-talk/shared';

       export const COMMANDS = {
         '/quit': { description: 'Exit Cling Talk' },
         '/users': { description: 'Show nearby users' },
         '/friends': { description: 'Show friend list' },
         '/chat': { description: 'Start chat with user' },
         '/settings': { description: 'Open settings' },
         '/help': { description: 'Show available commands' },
       } as const;

       export type CommandName = keyof typeof COMMANDS;

       export function parseInput(input: string): ParsedInput {
         const trimmed = input.trim();
         if (trimmed.startsWith('/')) {
           const [name, ...args] = trimmed.split(' ');
           if (name && name in COMMANDS) {
             return { type: 'command', name, args };
           }
           // Unknown command — still parse as command for error handling
           return { type: 'command', name: name ?? '', args };
         }
         return { type: 'message', content: trimmed };
       }

       export function isKnownCommand(name: string): name is CommandName {
         return name in COMMANDS;
       }
       ```

    2. Create `packages/client/src/commands/CommandParser.test.ts`:
       - Test `/quit` parses as `{ type: 'command', name: '/quit', args: [] }`
       - Test `/chat user#1234` parses as `{ type: 'command', name: '/chat', args: ['user#1234'] }`
       - Test `hello world` parses as `{ type: 'message', content: 'hello world' }`
       - Test `/unknown` parses as command (not message) for error handling
       - Test empty string parses as `{ type: 'message', content: '' }`
       - Test `isKnownCommand('/quit')` returns true
       - Test `isKnownCommand('/unknown')` returns false

    3. Create `packages/client/src/ui/theme.ts`:
       ```typescript
       import { USER_COLORS, THEME_COLORS } from '@cling-talk/shared';

       export const theme = {
         text: {
           primary: THEME_COLORS.primary,     // green
           secondary: 'gray',
           error: 'red',
           info: 'cyan',
         },
         ui: {
           separator: THEME_COLORS.separator,  // green
           statusBar: THEME_COLORS.statusBar,  // green
           prompt: 'green',
         },
         badge: {
           'Claude Code': '#B87FFF',
           'Codex': '#10A37F',
           'Gemini': '#4285F4',
           'Cursor': '#00D4AA',
         } as const,
       } as const;

       export function getUserColor(index: number): string {
         return USER_COLORS[index % USER_COLORS.length]!;
       }

       export { USER_COLORS };
       ```

    4. Create `packages/client/bin/cling-talk.js`:
       ```javascript
       #!/usr/bin/env node
       import('../dist/index.js');
       ```
       Note: Ensure file has executable permission (chmod +x).

    5. Create `packages/client/src/index.tsx` (minimal placeholder for now — full App wiring in Plan 03):
       ```typescript
       import React from 'react';
       import { render } from 'ink';
       import { Text } from 'ink';

       function App() {
         return <Text color="green">Cling Talk v0.1.0 - Foundation</Text>;
       }

       render(<App />);
       ```

    6. Run `chmod +x packages/client/bin/cling-talk.js`

    7. Run `npx vitest run` to confirm all tests pass (identity + command parser)
  </action>
  <verify>
    <automated>cd /Users/jamie/Jamie_Dev/claude_chat && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `packages/client/src/commands/CommandParser.ts` contains `export function parseInput(input: string): ParsedInput`
    - `packages/client/src/commands/CommandParser.ts` contains `'/quit': { description: 'Exit Cling Talk' }`
    - `packages/client/src/commands/CommandParser.test.ts` exits 0
    - `packages/client/src/ui/theme.ts` contains `primary: 'green'` (retro terminal theme per CONTEXT.md)
    - `packages/client/src/ui/theme.ts` contains `'Claude Code': '#B87FFF'`
    - `packages/client/bin/cling-talk.js` starts with `#!/usr/bin/env node`
    - `packages/client/src/index.tsx` contains `import { render } from 'ink'`
    - `npx vitest run` exits 0 with all tests passing
  </acceptance_criteria>
  <done>
    Command parser with tests, color theme with badge colors and user colors, bin entry with shebang, minimal index.tsx placeholder. All tests green.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run` — all tests pass (identity + command parser)
- `packages/shared/src/types.ts` exports Identity, AiCli, ChatMessage, SlashCommand, ParsedInput
- `packages/client/package.json` has correct bin field and dependencies
- TypeScript compiles without errors: `npx tsc --noEmit` from root
</verification>

<success_criteria>
- Monorepo structure works with npm workspaces (npm install succeeds)
- Shared types are importable from client package
- Identity generation produces valid nick#XXXX format
- Identity persists via conf and round-trips correctly
- Command parser correctly distinguishes /commands from messages
- All unit tests pass
- bin entry has correct shebang for npx execution
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
