---
phase: 16-shared-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/ui/components/AiCliSelector.tsx
  - packages/client/src/ui/screens/OnboardingScreen.tsx
  - packages/client/tsdown.config.ts
  - packages/client/src/ui/screens/ChatScreen.tsx
  - packages/client/src/globals.d.ts
autonomous: true
requirements: [INFR-02, INFR-03]

must_haves:
  truths:
    - "AiCliSelector의 useInput이 isActive prop으로 활성 상태일 때만 키 이벤트를 처리한다"
    - "OnboardingScreen에서 ai-cli step일 때만 AiCliSelector가 키 이벤트를 받는다"
    - "__APP_VERSION__이 빌드타임에 package.json 버전으로 주입된다"
    - "ChatScreen의 하드코딩 'v0.1.0'이 __APP_VERSION__으로 대체된다"
  artifacts:
    - path: "packages/client/src/ui/components/AiCliSelector.tsx"
      provides: "isActive-guarded AI CLI selector"
      contains: "isActive"
    - path: "packages/client/tsdown.config.ts"
      provides: "Build-time version injection via define"
      contains: "__APP_VERSION__"
    - path: "packages/client/src/globals.d.ts"
      provides: "TypeScript declaration for __APP_VERSION__"
      contains: "declare const __APP_VERSION__"
  key_links:
    - from: "packages/client/src/ui/screens/OnboardingScreen.tsx"
      to: "packages/client/src/ui/components/AiCliSelector.tsx"
      via: "isActive prop based on step state"
      pattern: "isActive=.*step.*ai-cli"
    - from: "packages/client/tsdown.config.ts"
      to: "packages/client/src/ui/screens/ChatScreen.tsx"
      via: "define replaces __APP_VERSION__ at build time"
      pattern: "__APP_VERSION__"
---

<objective>
AiCliSelector의 useInput 키 이벤트 충돌을 isActive prop으로 수정하고, package.json 버전을 빌드타임에 주입하여 하드코딩 버전을 제거한다.

Purpose: 키 이벤트 충돌은 Phase 17 온보딩 개선 시 새 interactive 컴포넌트 추가에 필수 선행. 버전 주입은 Phase 18 WelcomeSection 버전 표시에 필수.
Output: isActive-guarded AiCliSelector, 빌드타임 __APP_VERSION__ 상수, ChatScreen 버전 교체
</objective>

<execution_context>
@/Users/jamie/Jamie_Dev/claude_chat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamie/Jamie_Dev/claude_chat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-shared-infrastructure/16-CONTEXT.md

@packages/client/src/ui/components/AiCliSelector.tsx
@packages/client/src/ui/screens/OnboardingScreen.tsx
@packages/client/tsdown.config.ts
@packages/client/src/ui/screens/ChatScreen.tsx

<interfaces>
<!-- Existing useInput pattern from Ink — other components already use isActive correctly -->
From packages/client/src/ui/components/UserList.tsx:
```typescript
useInput((input, key) => { ... }, { isActive: visible });
```

From packages/client/src/ui/components/FriendList.tsx:
```typescript
useInput((input, key) => { ... }, { isActive: visible });
```

From packages/client/src/ui/components/SettingsOverlay.tsx:
```typescript
useInput((_input, key) => { ... }, { isActive: visible });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AiCliSelector에 isActive prop 추가 + OnboardingScreen 연동</name>
  <files>packages/client/src/ui/components/AiCliSelector.tsx, packages/client/src/ui/screens/OnboardingScreen.tsx</files>
  <read_first>
    - packages/client/src/ui/components/AiCliSelector.tsx (전체 — 특히 line 6-8 interface, line 13 useInput 호출)
    - packages/client/src/ui/screens/OnboardingScreen.tsx (전체 — 특히 line 79 AiCliSelector 사용)
    - packages/client/src/ui/components/UserList.tsx (isActive 패턴 참고 — useInput의 두 번째 인자)
  </read_first>
  <action>
1. `AiCliSelector.tsx` 수정:
   - `AiCliSelectorProps` interface에 `isActive?: boolean` 추가 (optional, 기본값 true):
     ```typescript
     interface AiCliSelectorProps {
       onSelect: (aiCli: AiCli) => void;
       isActive?: boolean;
     }
     ```
   - component 함수 시그니처: `{ onSelect, isActive = true }`
   - line 13의 `useInput((input, key) => {` 호출에 두 번째 인자 추가:
     ```typescript
     useInput((input, key) => {
       // ...existing handler code unchanged...
     }, { isActive });
     ```

2. `OnboardingScreen.tsx` 수정:
   - line 79의 `<AiCliSelector onSelect={handleAiCliSelect} />` 를:
     ```typescript
     <AiCliSelector onSelect={handleAiCliSelect} isActive={step === 'ai-cli'} />
     ```
     로 변경. 현재 구조상 ai-cli step에서만 렌더링되므로 isActive는 항상 true이지만, 향후 Phase 17에서 OnboardingScreen 레이아웃을 리팩토링할 때 (step별 조건부 렌더링 → 단일 레이아웃 내 조건부 content) 안전성 보장을 위해 명시적으로 전달.
  </action>
  <verify>
    <automated>cd /Users/jamie/Jamie_Dev/hive_chat && grep -n "isActive" packages/client/src/ui/components/AiCliSelector.tsx; echo "---"; grep -n "isActive" packages/client/src/ui/screens/OnboardingScreen.tsx; echo "---"; npx tsc --noEmit -p packages/client/tsconfig.json 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - AiCliSelector.tsx contains `isActive?: boolean` in interface
    - AiCliSelector.tsx contains `{ isActive }` as second argument to useInput
    - AiCliSelector.tsx contains `isActive = true` in destructuring (default value)
    - OnboardingScreen.tsx contains `isActive={step === 'ai-cli'}`
    - TypeScript compilation exits with 0 errors
  </acceptance_criteria>
  <done>AiCliSelector가 isActive prop을 받아 useInput에 전달하고, OnboardingScreen이 step 상태 기반으로 isActive를 제어</done>
</task>

<task type="auto">
  <name>Task 2: 빌드타임 버전 주입 + ChatScreen 하드코딩 버전 제거</name>
  <files>packages/client/tsdown.config.ts, packages/client/src/globals.d.ts, packages/client/src/ui/screens/ChatScreen.tsx</files>
  <read_first>
    - packages/client/tsdown.config.ts (전체 — 현재 빌드 설정)
    - packages/client/src/ui/screens/ChatScreen.tsx (line 116 — 'HiveChat v0.1.0' 하드코딩)
    - packages/client/package.json (version 필드 확인)
  </read_first>
  <action>
1. `packages/client/tsdown.config.ts` 수정:
   - 파일 상단에 package.json import 추가:
     ```typescript
     import { readFileSync } from 'node:fs'
     const pkg = JSON.parse(readFileSync(new URL('./package.json', import.meta.url), 'utf-8')) as { version: string }
     ```
   - defineConfig 내부에 `define` 옵션 추가:
     ```typescript
     export default defineConfig({
       entry: ['src/index.tsx'],
       format: 'esm',
       outDir: 'dist',
       define: {
         '__APP_VERSION__': JSON.stringify(pkg.version),
       },
       deps: {
         neverBundle: [ ...existing... ],
       },
     })
     ```

2. `packages/client/src/globals.d.ts` 신규 생성:
   ```typescript
   declare const __APP_VERSION__: string;
   ```

3. `packages/client/src/ui/screens/ChatScreen.tsx` 수정:
   - line 116의 `addSystemMessage('HiveChat v0.1.0');` 를:
     ```typescript
     addSystemMessage(`HiveChat v${__APP_VERSION__}`);
     ```
     로 변경. 다른 코드는 변경하지 않음.

4. `packages/client/tsconfig.json` 확인:
   - `include`에 `src/globals.d.ts`가 포함되는지 확인 (일반적으로 `src/**/*` 패턴이면 자동 포함)
   - 포함되지 않으면 `include` 배열에 `"src/globals.d.ts"` 추가
  </action>
  <verify>
    <automated>cd /Users/jamie/Jamie_Dev/hive_chat && grep -n "__APP_VERSION__" packages/client/tsdown.config.ts packages/client/src/globals.d.ts packages/client/src/ui/screens/ChatScreen.tsx; echo "---"; grep -c "v0.1.0" packages/client/src/ui/screens/ChatScreen.tsx; echo "---"; npx tsc --noEmit -p packages/client/tsconfig.json 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - packages/client/tsdown.config.ts contains `'__APP_VERSION__': JSON.stringify(pkg.version)`
    - packages/client/tsdown.config.ts contains `readFileSync` for package.json
    - packages/client/src/globals.d.ts contains `declare const __APP_VERSION__: string`
    - packages/client/src/ui/screens/ChatScreen.tsx contains `__APP_VERSION__` (not `v0.1.0`)
    - `grep -c "v0.1.0" packages/client/src/ui/screens/ChatScreen.tsx` outputs `0` (hardcoded version removed)
    - TypeScript compilation exits with 0 errors
  </acceptance_criteria>
  <done>tsdown이 빌드 시 __APP_VERSION__을 package.json 버전으로 치환하고, ChatScreen의 하드코딩 버전이 제거되며, TypeScript 컴파일 성공</done>
</task>

</tasks>

<verification>
1. TypeScript 컴파일: `npx tsc --noEmit -p packages/client/tsconfig.json` — 에러 0
2. AiCliSelector에 `isActive` prop과 useInput guard 존재
3. OnboardingScreen에서 `isActive={step === 'ai-cli'}` 전달
4. tsdown.config.ts에 `define.__APP_VERSION__` 존재
5. ChatScreen.tsx에서 `v0.1.0` 문자열 완전 제거
6. `globals.d.ts`에 `__APP_VERSION__` 타입 선언 존재
</verification>

<success_criteria>
- AiCliSelector가 isActive=false일 때 키 이벤트를 처리하지 않음
- __APP_VERSION__이 빌드타임에 주입되어 런타임 파일 읽기 없이 버전 접근 가능
- ChatScreen의 하드코딩 'v0.1.0'이 제거됨
- TypeScript 컴파일 성공
</success_criteria>

<output>
After completion, create `.planning/phases/16-shared-infrastructure/16-02-SUMMARY.md`
</output>
