---
phase: 16-shared-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/shared/src/constants.ts
  - packages/client/src/hooks/useTerminalSize.ts
  - packages/client/src/ui/screens/ChatScreen.tsx
autonomous: true
requirements: [INFR-01, RESP-01]

must_haves:
  truths:
    - "useTerminalSize hook이 터미널 columns/rows/breakpoint를 반환한다"
    - "breakpoint가 compact(<80)/standard(80-120)/wide(>120) 3단계로 분류된다"
    - "터미널 크기 변경 시 resize 이벤트로 re-render가 trigger된다"
    - "ChatScreen이 useStdout 직접 사용 대신 useTerminalSize hook을 사용한다"
  artifacts:
    - path: "packages/client/src/hooks/useTerminalSize.ts"
      provides: "Terminal size detection + breakpoint classification hook"
      exports: ["useTerminalSize", "Breakpoint"]
    - path: "packages/shared/src/constants.ts"
      provides: "Breakpoint threshold constants"
      contains: "COMPACT_MAX_WIDTH"
  key_links:
    - from: "packages/client/src/hooks/useTerminalSize.ts"
      to: "packages/shared/src/constants.ts"
      via: "import breakpoint constants"
      pattern: "import.*COMPACT_MAX_WIDTH.*from.*@hivechat/shared"
    - from: "packages/client/src/ui/screens/ChatScreen.tsx"
      to: "packages/client/src/hooks/useTerminalSize.ts"
      via: "import and use hook"
      pattern: "useTerminalSize\\(\\)"
---

<objective>
useTerminalSize custom hook을 생성하고 breakpoint 시스템을 구축하여, 모든 UI 컴포넌트가 터미널 크기에 반응할 수 있는 기반을 마련한다.

Purpose: Phase 17-19의 반응형 UI 작업이 이 hook에 의존. 터미널 resize 감지 + breakpoint 분류가 선행되어야 한다.
Output: useTerminalSize hook, breakpoint 상수, ChatScreen 통합
</objective>

<execution_context>
@/Users/jamie/Jamie_Dev/claude_chat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamie/Jamie_Dev/claude_chat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-shared-infrastructure/16-CONTEXT.md

@packages/shared/src/constants.ts
@packages/client/src/ui/screens/ChatScreen.tsx
@packages/client/src/hooks/useServerConnection.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Breakpoint 상수 정의 + useTerminalSize hook 생성</name>
  <files>packages/shared/src/constants.ts, packages/client/src/hooks/useTerminalSize.ts</files>
  <read_first>
    - packages/shared/src/constants.ts (기존 상수 패턴 확인)
    - packages/client/src/hooks/useServerConnection.ts (기존 hook 패턴 확인)
    - packages/client/src/ui/screens/ChatScreen.tsx (현재 useStdout 사용 패턴 — lines 42-44)
  </read_first>
  <action>
1. `packages/shared/src/constants.ts`에 breakpoint 상수 추가:
   ```typescript
   // Breakpoint constants
   export const COMPACT_MAX_WIDTH = 80;
   export const WIDE_MIN_WIDTH = 120;
   export const DEFAULT_TERMINAL_ROWS = 24;
   ```
   기존 `DEFAULT_TERMINAL_WIDTH = 80`과 `MIN_TERMINAL_WIDTH = 60`은 유지.

2. `packages/client/src/hooks/useTerminalSize.ts` 신규 생성:
   - `Breakpoint` type export: `'compact' | 'standard' | 'wide'`
   - `TerminalSize` interface export: `{ columns: number; rows: number; breakpoint: Breakpoint }`
   - `getBreakpoint(columns: number): Breakpoint` 함수 export (테스트 용이성):
     - `columns < COMPACT_MAX_WIDTH` → `'compact'`
     - `columns >= WIDE_MIN_WIDTH` → `'wide'`
     - 그 외 → `'standard'`
   - `useTerminalSize(): TerminalSize` hook:
     - `useStdout()`에서 stdout 가져오기
     - `useState`로 `{ columns, rows }` 관리, 초기값: `stdout?.columns ?? DEFAULT_TERMINAL_WIDTH`, `stdout?.rows ?? DEFAULT_TERMINAL_ROWS`
     - `useEffect`로 `stdout.on('resize', handler)` 등록, cleanup에서 `stdout.off('resize', handler)` 제거
     - resize handler: `setSize({ columns: stdout.columns, rows: stdout.rows })`
     - 반환: `{ columns: size.columns, rows: size.rows, breakpoint: getBreakpoint(size.columns) }`
   - Import: `useStdout` from `ink`, `useState`/`useEffect` from `react`, 상수는 `@hivechat/shared`

3. `packages/shared/src/index.ts`에 새 상수 export 확인 (이미 barrel export 사용 중이면 자동 포함, 아니면 추가).
  </action>
  <verify>
    <automated>cd /Users/jamie/Jamie_Dev/hive_chat && npx vitest run --reporter=verbose 2>&1 | tail -20; echo "---"; grep -n "COMPACT_MAX_WIDTH\|WIDE_MIN_WIDTH\|DEFAULT_TERMINAL_ROWS" packages/shared/src/constants.ts; echo "---"; grep -n "export.*useTerminalSize\|export.*Breakpoint\|export.*getBreakpoint" packages/client/src/hooks/useTerminalSize.ts</automated>
  </verify>
  <acceptance_criteria>
    - packages/shared/src/constants.ts contains `export const COMPACT_MAX_WIDTH = 80;`
    - packages/shared/src/constants.ts contains `export const WIDE_MIN_WIDTH = 120;`
    - packages/shared/src/constants.ts contains `export const DEFAULT_TERMINAL_ROWS = 24;`
    - packages/client/src/hooks/useTerminalSize.ts contains `export type Breakpoint =`
    - packages/client/src/hooks/useTerminalSize.ts contains `export function useTerminalSize()`
    - packages/client/src/hooks/useTerminalSize.ts contains `export function getBreakpoint(`
    - packages/client/src/hooks/useTerminalSize.ts contains `stdout.on('resize'`
    - packages/client/src/hooks/useTerminalSize.ts contains `stdout.off('resize'`
    - packages/client/src/hooks/useTerminalSize.ts contains `import.*COMPACT_MAX_WIDTH.*from.*@hivechat/shared`
  </acceptance_criteria>
  <done>useTerminalSize hook이 Breakpoint type과 함께 export되고, resize 이벤트 리스너가 등록/해제되며, breakpoint 상수가 shared 패키지에 정의됨</done>
</task>

<task type="auto">
  <name>Task 2: ChatScreen에 useTerminalSize hook 통합</name>
  <files>packages/client/src/ui/screens/ChatScreen.tsx</files>
  <read_first>
    - packages/client/src/ui/screens/ChatScreen.tsx (전체 파일 — 특히 line 2 useStdout import, line 42-44 stdout 사용, line 84 messageAreaHeight 계산)
    - packages/client/src/hooks/useTerminalSize.ts (Task 1에서 생성한 hook)
  </read_first>
  <action>
1. ChatScreen.tsx 수정:
   - `useStdout` import 제거 (line 2의 `{ Box, Text, useInput, useStdout }` → `{ Box, Text, useInput }`)
   - `useTerminalSize` import 추가: `import { useTerminalSize } from '../../hooks/useTerminalSize.js';`
   - line 42-44의 기존 코드:
     ```typescript
     const { stdout } = useStdout();
     const rows = stdout?.rows ?? 24;
     const columns = stdout?.columns ?? DEFAULT_TERMINAL_WIDTH;
     ```
     를 다음으로 교체:
     ```typescript
     const { rows, columns, breakpoint } = useTerminalSize();
     ```
   - `DEFAULT_TERMINAL_WIDTH` import도 제거 (더 이상 ChatScreen에서 직접 사용하지 않음). 단, `MAX_MESSAGES`는 유지.
   - line 4의 import를 `import { MAX_MESSAGES } from '@hivechat/shared';`로 수정 (DEFAULT_TERMINAL_WIDTH 제거).
   - `breakpoint` 변수는 현재 plan에서는 사용하지 않지만 destructure하여 준비. Phase 17-19에서 하위 컴포넌트에 전달 예정.

2. 기존 동작 변경 없음 확인:
   - `rows`, `columns` 변수의 사용처는 모두 동일하게 작동
   - `messageAreaHeight` 계산 (line 84)은 변경 없음
   - `separator` (line 301)의 columns 사용도 동일
  </action>
  <verify>
    <automated>cd /Users/jamie/Jamie_Dev/hive_chat && npx tsc --noEmit -p packages/client/tsconfig.json 2>&1 | head -20; echo "---"; grep -n "useTerminalSize\|useStdout\|DEFAULT_TERMINAL_WIDTH" packages/client/src/ui/screens/ChatScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ChatScreen.tsx contains `import { useTerminalSize }` (not useStdout)
    - ChatScreen.tsx contains `const { rows, columns, breakpoint } = useTerminalSize()`
    - ChatScreen.tsx does NOT contain `useStdout`
    - ChatScreen.tsx does NOT contain `DEFAULT_TERMINAL_WIDTH` in import
    - ChatScreen.tsx import from `@hivechat/shared` contains `MAX_MESSAGES` but not `DEFAULT_TERMINAL_WIDTH`
    - TypeScript compilation (`npx tsc --noEmit -p packages/client/tsconfig.json`) exits with 0 errors
  </acceptance_criteria>
  <done>ChatScreen이 useTerminalSize hook을 사용하여 rows/columns/breakpoint를 가져오고, useStdout 직접 사용이 제거되며, TypeScript 컴파일 성공</done>
</task>

</tasks>

<verification>
1. TypeScript 컴파일: `npx tsc --noEmit -p packages/client/tsconfig.json` — 에러 0
2. Hook export 확인: `useTerminalSize`, `Breakpoint`, `getBreakpoint` 모두 export
3. ChatScreen에서 `useStdout` 참조 완전 제거 확인
4. Breakpoint 상수 3개가 shared/constants.ts에 존재
</verification>

<success_criteria>
- useTerminalSize hook이 { rows, columns, breakpoint } 반환
- breakpoint가 compact/standard/wide 3단계 분류
- resize 이벤트 리스너 등록/해제 포함
- ChatScreen이 useTerminalSize를 사용하고 useStdout 직접 사용 제거
- TypeScript 컴파일 성공
</success_criteria>

<output>
After completion, create `.planning/phases/16-shared-infrastructure/16-01-SUMMARY.md`
</output>
