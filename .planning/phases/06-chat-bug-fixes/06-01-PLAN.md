---
phase: 06-chat-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/ui/components/IMETextInput.tsx
  - packages/client/src/ui/components/IMETextInput.test.tsx
autonomous: true
requirements:
  - BUG-01

must_haves:
  truths:
    - "터미널 폭보다 긴 텍스트를 입력해도 현재 입력 중인 내용이 항상 보인다"
    - "커서는 항상 입력 끝에 위치하고 텍스트가 좌로 밀린다"
    - "한글/CJK 문자 입력 시에도 sliding window가 정확하게 동작한다"
  artifacts:
    - path: "packages/client/src/ui/components/IMETextInput.tsx"
      provides: "Sliding window input with overflow handling"
      contains: "getVisibleText"
    - path: "packages/client/src/ui/components/IMETextInput.test.tsx"
      provides: "Tests for sliding window behavior"
      contains: "getVisibleText"
  key_links:
    - from: "IMETextInput.tsx"
      to: "string-width"
      via: "stringWidth() for CJK-aware visible window calculation"
      pattern: "stringWidth"
---

<objective>
IMETextInput에 sliding window 방식의 수평 스크롤을 구현하여 터미널 폭보다 긴 텍스트 입력 시에도 현재 타이핑 중인 내용이 항상 보이도록 한다.

Purpose: BUG-01 — 긴 텍스트 입력 시 입력 내용이 사라지는 버그 수정
Output: sliding window가 적용된 IMETextInput 컴포넌트 + 테스트
</objective>

<execution_context>
@/Users/jamie/Jamie_Dev/claude_chat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamie/Jamie_Dev/claude_chat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-chat-bug-fixes/06-CONTEXT.md

<interfaces>
<!-- IMETextInput 현재 구조 -->

From packages/client/src/ui/components/IMETextInput.tsx:
```typescript
const PROMPT = '> ';

export function calcCursorX(text: string, prompt: string = PROMPT): number;

interface IMETextInputProps {
  onSubmit: (text: string) => void;
  placeholder?: string;
  allowEmpty?: boolean;
  showCursor?: boolean;
  isActive?: boolean;
}

export function IMETextInput({ onSubmit, placeholder, allowEmpty, showCursor, isActive }: IMETextInputProps): JSX.Element;
```

From packages/client/src/ui/screens/ChatScreen.tsx (consumer):
```typescript
// L275-279: IMETextInput usage
<IMETextInput
  onSubmit={handleSubmit}
  placeholder={inputPlaceholder}
  isActive={!isInputDisabled && !showUserList && !showFriendList && !incomingRequest}
/>
```

From packages/shared/src/constants.ts:
```typescript
export const DEFAULT_TERMINAL_WIDTH = 80;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getVisibleText 함수 구현 및 테스트</name>
  <files>packages/client/src/ui/components/IMETextInput.tsx, packages/client/src/ui/components/IMETextInput.test.tsx</files>
  <read_first>
    - packages/client/src/ui/components/IMETextInput.tsx
    - packages/client/src/ui/components/IMETextInput.test.tsx
    - .claude/rules/tui-cjk-input.md
  </read_first>
  <behavior>
    - getVisibleText("hello world", 5) returns "world" (마지막 5 column 폭만큼 표시)
    - getVisibleText("short", 20) returns "short" (텍스트가 가용 폭보다 짧으면 전체 반환)
    - getVisibleText("abc한글def", 6) returns "글def" (CJK 2-column 폭 고려, 끝에서 6 column)
    - getVisibleText("", 20) returns "" (빈 문자열)
    - getVisibleText("한글한글한글", 5) returns "한글" (5 column에 CJK 2글자=4 column만 들어감, 1 column은 남음)
    - getVisibleText("a한b", 3) returns "한b" 또는 가용 폭 내 최대 — CJK가 경계에 걸리면 들어가는 만큼만
  </behavior>
  <action>
1. IMETextInput.test.tsx에 `getVisibleText` 테스트 추가:
   - `describe('getVisibleText')` 블록 생성
   - 위 behavior에 명시된 케이스들 테스트 작성
   - `import { getVisibleText } from './IMETextInput.js'` 추가

2. IMETextInput.tsx에 `getVisibleText` 함수 구현 (export):
   ```typescript
   export function getVisibleText(text: string, availableWidth: number): string {
     // stringWidth로 전체 폭 계산
     // 전체 폭 <= availableWidth이면 text 그대로 반환
     // 그렇지 않으면 끝에서부터 availableWidth에 맞는 문자들만 추출
     // Array.from(text)로 grapheme 단위 순회, 뒤에서부터 누적 폭 계산
     // CJK 문자가 경계에 걸리면 해당 문자 제외 (폭 초과 방지)
   }
   ```

3. 테스트 실행하여 RED → GREEN 확인
  </action>
  <verify>
    <automated>cd /Users/jamie/Jamie_Dev/cling_talk && npx vitest run packages/client/src/ui/components/IMETextInput.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - IMETextInput.tsx contains `export function getVisibleText(`
    - getVisibleText uses `stringWidth` (not `.length`) for width calculation
    - IMETextInput.test.tsx contains `describe('getVisibleText'`
    - All getVisibleText tests pass (ASCII, CJK, mixed, empty, boundary cases)
  </acceptance_criteria>
  <done>getVisibleText 함수가 ASCII/CJK/혼합 문자열에 대해 정확한 sliding window 결과를 반환하고 모든 테스트 통과</done>
</task>

<task type="auto">
  <name>Task 2: IMETextInput 컴포넌트에 sliding window 적용</name>
  <files>packages/client/src/ui/components/IMETextInput.tsx</files>
  <read_first>
    - packages/client/src/ui/components/IMETextInput.tsx
    - packages/client/src/ui/screens/ChatScreen.tsx
    - packages/shared/src/constants.ts
  </read_first>
  <action>
1. IMETextInput 컴포넌트의 렌더링 부분을 수정하여 sliding window 적용:

   a. `useStdout`를 ink에서 import 추가:
      `import { Box, Text, useInput, useStdout } from 'ink';`

   b. 컴포넌트 함수 내부에서 터미널 폭 가져오기:
      ```typescript
      const { stdout } = useStdout();
      const columns = stdout?.columns ?? DEFAULT_TERMINAL_WIDTH;
      ```

   c. `DEFAULT_TERMINAL_WIDTH`를 shared에서 import:
      `import { DEFAULT_TERMINAL_WIDTH } from '@cling-talk/shared';`

   d. 가용 폭 계산 (CONTEXT.md 결정사항: `columns - stringWidth(PROMPT) - 1`):
      ```typescript
      const availableWidth = columns - stringWidth(PROMPT) - 1; // 1 for cursor
      ```

   e. 렌더링 시 `text` 대신 `getVisibleText(text, availableWidth)` 사용:
      기존: `<Text>{text}</Text>`
      변경: `<Text>{getVisibleText(text, availableWidth)}</Text>`

2. 오버플로우 인디케이터 (선택): 텍스트가 잘릴 때 맨 앞에 `…` 표시는 하지 않음 — 채팅 UX에서 깔끔함 우선 (CONTEXT.md에 길이 표시 안 함 결정)

3. 기존 IMETextInputProps 인터페이스는 변경 없음 — ChatScreen.tsx의 사용 방식과 호환 유지
  </action>
  <verify>
    <automated>cd /Users/jamie/Jamie_Dev/cling_talk && npx vitest run packages/client/src/ui/components/IMETextInput.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - IMETextInput.tsx contains `import { Box, Text, useInput, useStdout } from 'ink'`
    - IMETextInput.tsx contains `import { DEFAULT_TERMINAL_WIDTH } from '@cling-talk/shared'`
    - IMETextInput.tsx contains `getVisibleText(text, availableWidth)`
    - IMETextInput.tsx contains `columns - stringWidth(PROMPT) - 1`
    - IMETextInputProps interface is unchanged (no new required props)
    - All existing tests still pass
  </acceptance_criteria>
  <done>IMETextInput이 터미널 폭에 맞게 sliding window로 텍스트를 표시하며, 기존 API 호환성 유지, 모든 테스트 통과</done>
</task>

</tasks>

<verification>
- `npx vitest run packages/client/src/ui/components/IMETextInput.test.tsx` — 모든 테스트 통과
- `npx tsc --noEmit -p packages/client/tsconfig.json` — 타입 에러 없음
- 수동 확인: `npx cling-talk` 실행 후 80 column 터미널에서 100자 이상 입력 시 텍스트가 사라지지 않고 끝부분이 보임
</verification>

<success_criteria>
- 터미널 폭보다 긴 텍스트 입력 시 커서 근처 텍스트가 항상 보인다
- CJK 문자열도 정확한 폭 계산으로 sliding window 동작한다
- 기존 IMETextInput API 변경 없이 ChatScreen과 호환된다
</success_criteria>

<output>
After completion, create `.planning/phases/06-chat-bug-fixes/06-01-SUMMARY.md`
</output>
