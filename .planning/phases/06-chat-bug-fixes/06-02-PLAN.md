---
phase: 06-chat-bug-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/client/src/ui/components/MessageArea.tsx
  - packages/client/src/ui/components/MessageArea.test.tsx
  - packages/client/src/ui/screens/ChatScreen.tsx
autonomous: true
requirements:
  - BUG-02

must_haves:
  truths:
    - "메시지가 50개 이상 쌓여도 채팅 영역이 최신 메시지 방향으로 정상 스크롤된다"
    - "스크롤 후에도 상단에 이전 메시지가 고정되어 남는 현상이 발생하지 않는다"
    - "Page Up/Down으로 이전 메시지를 탐색할 수 있다"
    - "스크롤 중 새 메시지 도착 시 위치 유지 + 알림 표시"
    - "ChatScreen의 메시지 버퍼가 500개로 제한된다"
  artifacts:
    - path: "packages/client/src/ui/components/MessageArea.tsx"
      provides: "Scrollable message area with Page Up/Down and auto-scroll"
      contains: "scrollOffset"
    - path: "packages/client/src/ui/components/MessageArea.test.tsx"
      provides: "Tests for message visibility calculation"
      contains: "calcVisibleMessages"
    - path: "packages/client/src/ui/screens/ChatScreen.tsx"
      provides: "Message buffer limited to 500 + height passed to MessageArea"
      contains: "MAX_MESSAGES"
  key_links:
    - from: "ChatScreen.tsx"
      to: "MessageArea.tsx"
      via: "availableHeight prop and message buffer limit"
      pattern: "availableHeight"
    - from: "MessageArea.tsx"
      to: "useInput"
      via: "Page Up/Down keyboard handling for scroll"
      pattern: "pageUp|pageDown"
---

<objective>
MessageArea에 스크롤 메커니즘을 구현하고 ChatScreen의 메시지 버퍼를 500개로 제한하여, 메시지가 많아져도 최신 메시지가 정상 표시되고 이전 메시지를 탐색할 수 있도록 한다.

Purpose: BUG-02 — 메시지 영역 상단 고정 현상 수정 + 스크롤 지원
Output: 스크롤 가능한 MessageArea + 버퍼 제한된 ChatScreen
</objective>

<execution_context>
@/Users/jamie/Jamie_Dev/claude_chat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jamie/Jamie_Dev/claude_chat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-chat-bug-fixes/06-CONTEXT.md

<interfaces>
<!-- MessageArea 현재 구조 -->

From packages/client/src/ui/components/MessageArea.tsx:
```typescript
interface MessageAreaProps {
  messages: ChatMessage[];
  myIdentity?: Identity;
}

export function MessageArea({ messages, myIdentity }: MessageAreaProps): JSX.Element;
```

From packages/client/src/ui/screens/ChatScreen.tsx:
```typescript
// L37-39: terminal dimensions
const { stdout } = useStdout();
const rows = stdout?.rows ?? 24;
const columns = stdout?.columns ?? DEFAULT_TERMINAL_WIDTH;

// L202: message addition (no buffer limit)
setMessages(prev => [...prev, msg]);

// L61: system message addition (no buffer limit)
setMessages(prev => [...prev, sysMsg]);

// L263: MessageArea usage
<MessageArea messages={displayMessages} myIdentity={identity} />

// L224-281: Layout structure
// StatusBar (1 row) → separator (1 row) → MessageArea (flexGrow) → separator (1 row) → IMETextInput (1 row)
```

From packages/shared/src/constants.ts:
```typescript
export const MAX_MESSAGES = 500;
```

From packages/client/src/ui/theme.ts:
```typescript
export const theme = {
  text: { primary: 'green', secondary: 'gray', error: 'red', info: 'cyan' },
  ui: { separator: 'green', statusBar: 'green', prompt: 'green' },
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MessageArea 스크롤 로직 구현</name>
  <files>packages/client/src/ui/components/MessageArea.tsx, packages/client/src/ui/components/MessageArea.test.tsx</files>
  <read_first>
    - packages/client/src/ui/components/MessageArea.tsx
    - packages/client/src/ui/screens/ChatScreen.tsx
    - packages/shared/src/constants.ts
    - .claude/rules/tui-cjk-input.md
  </read_first>
  <behavior>
    - calcVisibleMessages(10 messages, availableHeight=5, scrollOffset=0) returns last 5 messages (auto-scroll to bottom)
    - calcVisibleMessages(10 messages, availableHeight=5, scrollOffset=3) returns messages[2..7] (scrolled up 3)
    - calcVisibleMessages(3 messages, availableHeight=10, scrollOffset=0) returns all 3 messages (fewer than height)
    - calcVisibleMessages(0 messages, availableHeight=5, scrollOffset=0) returns empty array
    - scrollOffset cannot exceed messages.length - availableHeight (clamped)
    - scrollOffset cannot go below 0
  </behavior>
  <action>
1. MessageArea.test.tsx 생성 — `calcVisibleMessages` 순수 함수 테스트:
   ```typescript
   import { describe, it, expect } from 'vitest';
   import { calcVisibleMessages } from './MessageArea.js';

   describe('calcVisibleMessages', () => {
     // behavior에 명시된 케이스들 구현
   });
   ```
   테스트용 ChatMessage 헬퍼 함수 만들어서 사용 (id, from, content, timestamp만 있으면 됨)

2. MessageArea.tsx 수정:

   a. props 인터페이스 확장:
      ```typescript
      interface MessageAreaProps {
        messages: ChatMessage[];
        myIdentity?: Identity;
        availableHeight: number;  // 새로 추가: 표시 가능한 라인 수
        isActive?: boolean;       // 새로 추가: useInput 활성화 여부 (오버레이 표시 중 비활성)
      }
      ```

   b. `calcVisibleMessages` 순수 함수 export:
      ```typescript
      export function calcVisibleMessages(
        messages: ChatMessage[],
        availableHeight: number,
        scrollOffset: number
      ): ChatMessage[] {
        if (messages.length === 0 || availableHeight <= 0) return [];
        const maxOffset = Math.max(0, messages.length - availableHeight);
        const clampedOffset = Math.min(Math.max(0, scrollOffset), maxOffset);
        const startIndex = messages.length - availableHeight - clampedOffset;
        const safeStart = Math.max(0, startIndex);
        const endIndex = safeStart + Math.min(availableHeight, messages.length);
        return messages.slice(safeStart, endIndex);
      }
      ```

   c. 컴포넌트 내부에 scroll state + keyboard handling 추가:
      ```typescript
      import { Box, Text, useInput } from 'ink';

      const [scrollOffset, setScrollOffset] = useState(0);
      const prevMessageCount = useRef(messages.length);

      // 새 메시지 도착 시 auto-scroll (scrollOffset === 0일 때만)
      useEffect(() => {
        if (messages.length > prevMessageCount.current && scrollOffset === 0) {
          // 이미 하단에 있으므로 유지
        }
        prevMessageCount.current = messages.length;
      }, [messages.length, scrollOffset]);

      // Page Up/Down 처리
      useInput((input, key) => {
        if (key.pageUp) {
          setScrollOffset(prev => Math.min(prev + availableHeight, Math.max(0, messages.length - availableHeight)));
        }
        if (key.pageDown) {
          setScrollOffset(prev => Math.max(0, prev - availableHeight));
        }
      }, { isActive: isActive !== false });
      ```

   d. 렌더링에서 `calcVisibleMessages` 사용:
      기존: `const visibleMessages = messages.slice(-MAX_MESSAGES);`
      변경: `const visibleMessages = calcVisibleMessages(messages, availableHeight, scrollOffset);`

   e. 스크롤 중 새 메시지 알림 (scrollOffset > 0일 때):
      ```typescript
      {scrollOffset > 0 && (
        <Box justifyContent="center">
          <Text dimColor>↓ new messages</Text>
        </Box>
      )}
      ```
      이 알림은 MessageArea 하단에 표시 (availableHeight에서 1줄 차감)

   f. `overflow="hidden"` 유지하되, Ink의 flexGrow 대신 고정 높이로 변경:
      `<Box flexDirection="column" height={availableHeight} overflow="hidden">`

3. 테스트 실행하여 확인
  </action>
  <verify>
    <automated>cd /Users/jamie/Jamie_Dev/cling_talk && npx vitest run packages/client/src/ui/components/MessageArea.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - MessageArea.tsx contains `export function calcVisibleMessages(`
    - MessageArea.tsx contains `useState(0)` for scrollOffset
    - MessageArea.tsx contains `pageUp` and `pageDown` handling
    - MessageArea.tsx contains `availableHeight: number` in MessageAreaProps
    - MessageArea.tsx contains `↓ new messages` indicator when scrollOffset > 0
    - MessageArea.test.tsx exists and contains `describe('calcVisibleMessages'`
    - All tests pass
  </acceptance_criteria>
  <done>MessageArea가 availableHeight 기반으로 메시지를 슬라이스하고, Page Up/Down으로 스크롤 가능하며, 새 메시지 알림을 표시한다</done>
</task>

<task type="auto">
  <name>Task 2: ChatScreen 메시지 버퍼 제한 + MessageArea 연동</name>
  <files>packages/client/src/ui/screens/ChatScreen.tsx</files>
  <read_first>
    - packages/client/src/ui/screens/ChatScreen.tsx
    - packages/client/src/ui/components/MessageArea.tsx
    - packages/shared/src/constants.ts
  </read_first>
  <action>
1. 메시지 버퍼 500개 제한 적용 (CONTEXT.md 결정사항):

   a. `MAX_MESSAGES`를 shared에서 import (이미 MessageArea에서 사용 중이므로 ChatScreen에도 추가):
      `import { DEFAULT_TERMINAL_WIDTH, MAX_MESSAGES } from '@cling-talk/shared';`

   b. `addSystemMessage` 콜백에서 버퍼 제한:
      기존 (L61): `setMessages(prev => [...prev, sysMsg]);`
      변경: `setMessages(prev => [...prev, sysMsg].slice(-MAX_MESSAGES));`

   c. 일반 메시지 추가에서도 버퍼 제한:
      기존 (L202): `setMessages(prev => [...prev, msg]);`
      변경: `setMessages(prev => [...prev, msg].slice(-MAX_MESSAGES));`

2. availableHeight 계산 (CONTEXT.md: `rows - 4` — 상태바 1 + 구분선 2 + 입력 1):

   ```typescript
   const messageAreaHeight = rows - 4; // StatusBar(1) + separator(1) + separator(1) + IMETextInput(1)
   ```

3. MessageArea에 새 props 전달:

   기존 (L263): `<MessageArea messages={displayMessages} myIdentity={identity} />`
   변경:
   ```typescript
   <MessageArea
     messages={displayMessages}
     myIdentity={identity}
     availableHeight={messageAreaHeight}
     isActive={!showUserList && !showFriendList && !incomingRequest}
   />
   ```

4. MessageArea의 flexGrow 제거 대응 — ChatScreen 레이아웃에서 MessageArea 영역을 고정 높이로:
   MessageArea 내부에서 `height={availableHeight}`를 설정하므로 ChatScreen 쪽은 변경 불필요.
   단, MessageArea를 감싸는 부분에서 `flexGrow={1}`이 아닌 고정 크기가 사용되는지 확인.

5. TypeScript 타입 체크:
   `npx tsc --noEmit -p packages/client/tsconfig.json`
  </action>
  <verify>
    <automated>cd /Users/jamie/Jamie_Dev/cling_talk && npx tsc --noEmit -p packages/client/tsconfig.json && npx vitest run packages/client/src/ui/components/MessageArea.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ChatScreen.tsx contains `import { DEFAULT_TERMINAL_WIDTH, MAX_MESSAGES } from '@cling-talk/shared'`
    - ChatScreen.tsx contains `.slice(-MAX_MESSAGES)` in both setMessages calls (addSystemMessage and regular message)
    - ChatScreen.tsx contains `const messageAreaHeight = rows - 4`
    - ChatScreen.tsx contains `availableHeight={messageAreaHeight}` in MessageArea usage
    - ChatScreen.tsx contains `isActive={` in MessageArea usage
    - TypeScript compilation passes with no errors
    - All MessageArea tests pass
  </acceptance_criteria>
  <done>ChatScreen이 메시지 버퍼를 500개로 제한하고, MessageArea에 정확한 availableHeight를 전달하여 스크롤이 정상 동작한다</done>
</task>

</tasks>

<verification>
- `npx vitest run packages/client/src/ui/components/` — MessageArea + IMETextInput 테스트 모두 통과
- `npx tsc --noEmit -p packages/client/tsconfig.json` — 타입 에러 없음
- 수동 확인: `npx cling-talk` 실행 후 50개 이상 메시지 입력 시 최신 메시지가 하단에 보이고, Page Up/Down으로 이전 메시지 탐색 가능
</verification>

<success_criteria>
- 메시지가 50개 이상 쌓여도 최신 메시지가 하단에 정상 표시된다
- 상단에 이전 메시지가 고정되어 남는 현상이 없다
- Page Up/Down으로 이전 메시지를 탐색할 수 있다
- 스크롤 중 새 메시지 도착 시 위치 유지 + 알림 표시
- ChatScreen 메시지 버퍼가 500개로 제한된다
</success_criteria>

<output>
After completion, create `.planning/phases/06-chat-bug-fixes/06-02-SUMMARY.md`
</output>
